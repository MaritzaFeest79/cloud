# CloudLink 技术资源导航站

CloudLink 是一个面向开发人员、技术研究者与开源爱好者的高质量外链资源聚合与导航系统。项目定位于解决互联网技术信息碎片化、优质内容难以发现、外链管理混乱等问题，通过人工筛选与社区共建的方式，构建一个结构清晰、可维护、可扩展的技术资源目录。目标用户包括个人开发者、技术团队负责人、开源贡献者以及高校计算机相关专业师生。项目本身不存储任何第三方内容，仅提供指向外部资源的超链接，并辅以分类、标签与简注，帮助用户快速定位所需信息。

## 功能概览

- 分类资源索引：按技术领域、内容类型、适用人群等多维度对资源进行归类，支持扁平化标签与树状目录双模式浏览。
- 外链健康监测：自动对收录的 URL 进行可用性检查，标记失效链接并生成报告，保障导航质量。
- 用户投稿与审核：注册用户可提交新资源链接，经维护者审核通过后正式入库，支持提交理由与标签建议。
- 个性化收藏夹：登录用户可创建自定义资源列表，便于项目组或团队内部共享常用外链集合。
- 全文与标签检索：基于标题、描述、标签及域名关键词的快速搜索，支持模糊匹配与精确筛选。
- 导入导出标准格式：支持 OPML、JSON、CSV 格式的链接批量导入导出，便于与其他导航工具或书签系统互通。
- 访问统计与热度排序：记录每个外链的点击次数与最近访问时间，支持按热度、更新时间、新增时间排序浏览。

## 应用场景

1. 技术团队内部知识库外链管理：团队可将日常研发中参考的文档、工具、博客、API 参考等分散链接统一收录至 CloudLink，按项目或技术栈分类，新成员入职时可快速获取所有必需外部资源。

2. 开源项目文档中的资源附录：开源维护者可将 CloudLink 作为项目官网的“友情链接”或“相关资源”页面的后端数据源，通过 API 自动同步分类列表，避免在多个仓库中重复维护外链列表。

3. 技术社区与论坛的导航聚合：技术社区运营方可部署 CloudLink 作为社区的“导航”子站点，为成员提供精选的教程、视频、代码库、招聘信息等外部链接，提升社区内容服务的完整性。

4. 高校实验课与课程资料辅助：教师可将课程所需的在线编译器、数据集、论文库、官方标准文档等链接通过 CloudLink 统一发布，学生无需自行搜索，直接按分类访问，降低信息检索成本。

5. 个人开发者的书签替代方案：开发者可将日常积累的大量技术书签迁移至 CloudLink，利用标签和搜索功能替代浏览器自带的文件夹式管理，实现跨设备、跨浏览器的统一访问。

## 快速开始

以下步骤适用于 Linux / macOS / Windows (WSL) 环境，假设已安装 Git 与 Node.js (v18 LTS) 及 npm。

```bash
# 1. 克隆仓库
git clone https://github.com/cloudlink-dev/cloudlink.git
cd cloudlink

# 2. 安装项目依赖（包含前端与后端）
npm install

# 3. 以开发模式启动（前端热重载 + 后端 API 服务）
npm run dev
```

启动成功后，终端会输出本地访问地址（默认 http://localhost:3000）和 API 端点（默认 http://localhost:5000）。首次启动会自动初始化 SQLite 数据库并导入预设的基础分类与示例链接数据。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | 18.x LTS 或 20.x LTS | 运行时环境，推荐使用官方二进制或 nvm 安装 |
| npm | 9.x 或 10.x | 包管理器，随 Node.js 一并安装 |
| SQLite3 | 3.39.0 或更高 | 内嵌数据库，无需额外安装，项目使用 better-sqlite3 驱动 |
| Git | 2.30 或更高 | 用于克隆仓库及版本控制，非运行强制依赖但强烈建议 |
| 操作系统 | Linux (glibc 2.28+), macOS 11+, Windows 10/11 (WSL2) | 生产部署推荐 Linux 发行版（Ubuntu 22.04 / Debian 11） |
| 浏览器 | 现代浏览器（Chrome 110+, Firefox 110+, Edge 110+） | 仅对管理后台界面有要求，公开浏览端支持 IE11 及以上（不保证全部功能） |
| 可选 - Redis | 7.0+ | 若启用会话缓存与限流功能，需额外配置 Redis 连接 |
| 可选 - 邮件服务 | SMTP 兼容 | 用于用户注册验证与通知，需配置环境变量 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户使用 | /docs/user-guide/quick-start.md | 如何注册、登录、添加收藏、提交新链接、设置个人偏好？ |
| 管理员运维 | /docs/admin/deployment.md | 如何选择部署架构（单机/容器/集群）、配置反向代理、设置 SSL 证书？ |
| 开发者扩展 | /docs/developer/api-reference.md | 如何调用 RESTful API 进行二次开发、自定义分类导入规则、编写插件？ |
| 贡献者指南 | /docs/contributor/coding-standards.md | 代码风格要求、Git 提交规范、PR 流程、测试覆盖率标准是什么？ |
| 架构设计 | /docs/architecture/data-model.md | 数据库表结构设计、缓存策略、外链健康检查调度机制如何工作？ |
| 安全策略 | /docs/security/authentication.md | 密码加密方式、JWT 有效期、防 XSS 与 CSRF 措施、日志审计规则？ |

## 资源列表

本导航站已收录以下独立资源站点，按内容领域分节展示。所有 URL 均严格保持用户提供的原始格式，未做任何协议补全、域名规范化或路径修改。

### 个人创作与文学资源

- <code>wuyeyiren.org.cn</code>
- <code>wuyeshuangshuangshuang.org.cn</code>
- <code>tingtingyiren.org.cn</code>

### 综合资讯与导航类

- <code>oumeishiba.org.cn</code>
- <code>tingtingdaohang.org.cn</code>

### 垂直领域内容聚合

- <code>yirenguochannv.org.cn</code>
- <code>yazhouzhongwenzimutiantang.org.cn</code>

## 项目结构

项目采用前后端分离但同仓维护的 Monorepo 风格布局，核心目录及文件说明如下：

```
cloudlink/
├── backend/                         # 后端服务（Node.js + Express）
│   ├── src/
│   │   ├── api/                     # RESTful API 路由定义（v1 版本）
│   │   │   ├── auth.js              # 注册、登录、令牌刷新
│   │   │   ├── links.js             # 链接增删改查、分类管理
│   │   │   ├── health.js            # 健康检查与状态报告
│   │   │   └── user.js              # 个人资料与收藏操作
│   │   ├── models/                  # 数据模型层（SQLite ORM 映射）
│   │   │   ├── Link.js              # 外链实体模型
│   │   │   ├── Category.js          # 分类树模型
│   │   │   └── User.js              # 用户及凭证模型
│   │   ├── services/                # 业务逻辑服务层
│   │   │   ├── checker.js           # 定时外链可用性探测
│   │   │   ├── importer.js          # OPML/JSON 导入解析
│   │   │   └── stats.js             # 点击统计与排行计算
│   │   └── utils/                   # 通用工具函数（日志、加密、验证码）
│   ├── config/                      # 环境配置与常量定义
│   ├── tests/                       # 单元测试与集成测试用例
│   └── package.json                 # 后端依赖与脚本
├── frontend/                        # 前端单页应用（React + Vite）
│   ├── src/
│   │   ├── pages/                   # 页面级组件（首页、分类页、详情、登录）
│   │   ├── components/              # 可复用 UI 组件（导航栏、卡片、搜索框）
│   │   ├── hooks/                   # 自定义 React Hooks（请求、分页、防抖）
│   │   ├── stores/                  # Zustand 状态管理（用户、收藏、筛选）
│   │   └── styles/                  # 全局 CSS 与主题变量
│   ├── public/                      # 静态资源（favicon、robots.txt）
│   └── vite.config.js               # 构建工具配置与代理设置
├── docs/                            # 完整项目文档（见上文文档导航）
├── scripts/                         # 运维辅助脚本（备份、迁移、初始化）
├── .env.example                     # 环境变量模板文件
├── docker-compose.yml               # 容器编排（后端 + 前端 + Redis）
├── Dockerfile                       # 多阶段构建镜像文件
└── README.md                        # 本文档
```

## 贡献指南

我们欢迎任何形式的贡献，包括但不限于新增资源链接、修复文档错误、改进 UI 样式、优化后端性能、报告安全问题。请遵循以下步骤：

1. 查阅贡献者行为准则与编码规范文档（位于 /docs/contributor/code-of-conduct.md），确保您认同社区协作理念。

2. 在 GitHub Issues 中查找或新建一个与您贡献内容相关的问题（Feature Request / Bug Report），等待维护者确认或分配标签，避免重复工作。

3. Fork 本仓库至您的个人账号，创建一个新的功能分支（命名规则：feat/功能简述 或 fix/问题简述），在该分支上进行修改。

4. 本地运行 `npm run test` 和 `npm run lint` 确保所有现有测试通过且代码风格符合 ESLint 配置，若新增功能需补充对应的单元测试或 e2e 测试用例。

5. 提交 Pull Request 到主仓库的 develop 分支，PR 描述中需清楚说明变更目的、影响范围、测试情况，并关联相关 Issue 编号。PR 至少需要两名维护者 Approve 后方可合并。

## 常见问题

**Q: 外链健康检查会频繁访问目标站点，是否会对目标服务器造成压力？**

A: 系统默认每个外链的检查间隔为 24 小时，且采用随机延迟（±2 小时）分散请求峰值。每次检查仅发送一个 HTTP HEAD 请求（若 HEAD 不支持则回退为 GET 但限定只读取前 1KB 响应），超时时间设置为 5 秒，且检查器会遵循 robots.txt 规则。对于连续 3 次检查失败的链接，系统会自动降低检查频率至每 7 天一次，并标记为“待人工确认”。

**Q: 我是否可以完全离线部署 CloudLink，不依赖互联网？**

A: 可以。CloudLink 核心功能（浏览、分类、检索、收藏、导入导出）完全可在内网环境运行，不需要访问任何外部 API（除外链健康检查时会主动探测目标站点，此功能可配置关闭）。但需要注意，用户注册时的邮件验证功能依赖 SMTP 服务，若没有内部邮件服务器，可禁用该验证或使用 SQLite 本地存储验证码。前端字体与图标库可下载至本地托管。

**Q: 生产环境如何迁移数据库或备份数据？**

A: SQLite 数据库文件位于 `backend/data/cloudlink.db`，直接复制该文件即可完成完整备份。若需迁移至 PostgreSQL（社区版支持），需使用 `scripts/migrate-to-pg.js` 脚本，该脚本会读取 SQLite 数据并批量插入 PostgreSQL 表，同时保持外键关联与索引结构。强烈建议在生产部署前使用 `npm run backup` 命令配置每日自动备份任务。

## 许可证

MIT License. 详见项目根目录的 LICENSE 文件。

> 外链数量: 7 | 生成时间: 2026-08-11 03:44:12
