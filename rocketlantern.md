# BifenHub

BifenHub 是一个面向数据聚合与实时信息分发的开源技术资源导航平台，专注于将分散在网络各处的结构化数据源、比分直播接口、历史数据归档与实时状态监控资源进行统一归集与标准化输出。项目定位为技术开发者、数据爱好者与运维工程师提供一套可自托管的通用外链资源管理与状态巡检工具，解决数据源变动频繁、官方入口难以追溯、多源对比效率低下等实际问题。通过本平台，用户可快速建立自己的数据源书签体系，并利用内置的可用性检测与变更通知机制，持续跟踪关键资源的状态变化。

## 功能概览

- **多源统一看板**：提供集中化的资源卡片展示面板，将不同域名、不同协议的数据源入口以统一元数据格式呈现，支持按标签、按状态、按更新时间排序筛选。

- **可用性主动探测**：内置基于 HTTP 状态码与响应时间的周期性探测任务，对每个收录资源执行分钟级可用性检查，并在资源不可用时输出结构化告警日志。

- **变更追踪与比对**：记录每个资源入口的响应头、TLS 证书信息与页面摘要哈希，支持历史变更回溯与多版本差异比对，便于定位内容更新或接口改动。

- **自定义标签体系**：允许用户为每个资源打上多维度标签（如 实时、历史、移动端、桌面端、备用节点），并基于标签组合生成个性化视图。

- **开放式订阅接口**：提供 RESTful API 与 Webhook 订阅能力，允许第三方系统按需拉取资源列表、状态快照或变更事件，便于集成至现有监控或数据管道。

- **静态资源离线缓存**：支持将关键入口的静态说明文档、示例请求体与常见错误码说明缓存至本地，在网络抖动或目标源临时维护时仍可提供基础参考信息。

- **一键导出与迁移**：支持将当前配置的资源列表、标签体系与探测规则导出为 JSON 或 YAML 格式，便于在新实例中快速恢复或进行版本化管理。

## 应用场景

- **运维工程师搭建数据源状态看板**：运维人员可利用 BifenHub 快速建立内部数据源状态总览页，将多个外部数据入口统一纳入可用性监测范围，当某个数据源响应超时或返回异常状态码时，系统输出结构化告警日志并推送至企业微信群，缩短故障发现与定位时间。

- **数据分析师整理常用数据入口书签**：数据分析师在开展多源数据对比或历史趋势回溯工作时，可通过 BifenHub 分类存储不同维度的数据入口，并为每个入口添加业务标签（如 实时行情、历史归档、备用线路），配合变更追踪功能，可快速确认某个入口是否在近期发生过响应结构变化，避免因接口字段调整导致分析脚本报错。

- **开源社区维护公共资源导航仓库**：开源社区维护者可基于 BifenHub 构建面向特定领域（如体育数据、天气数据、交通数据）的公共资源导航站点，利用其开放的 API 接口将资源列表同步至社区文档或 CI 流水线中，实现资源增删改查的自动化同步，减少人工维护成本。

- **个人开发者自建轻量级聚合页**：个人开发者在开发多数据源聚合应用时，可使用 BifenHub 作为本地调试阶段的资源管理后台，快速切换不同数据源进行兼容性测试，并通过离线缓存功能在无网络环境下继续查阅已收录资源的接口说明与示例。

## 快速开始

以下操作基于 Linux / macOS 环境，假定已安装 Git、Node.js 与 npm。请根据实际操作系统调整包管理器命令。

```bash
# 1. 克隆代码仓库
git clone https://github.com/bifenhub/bifenhub.git
cd bifenhub

# 2. 安装项目依赖
npm install

# 3. 以开发模式运行本地服务
npm run dev
```

执行上述命令后，本地服务将在 http://localhost:3000 启动。打开浏览器访问该地址即可看到默认的资源看板界面。首次启动时系统会自动加载内置的示例资源列表，您可在设置页面中导入自定义资源或直接编辑 data/sources.json 文件。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | >= 18.0.0 | 运行时环境，用于执行服务端脚本与构建工具链 |
| npm | >= 9.0.0 | 包管理器，用于安装项目依赖及运行脚本命令 |
| SQLite | 3.x (内置) | 轻量级嵌入式数据库，用于存储资源元数据与探测历史记录 |
| Git | >= 2.30.0 | 版本控制工具，用于克隆仓库及后续拉取更新 |
| 网络连通性 | 出站可达 | 用于执行可用性探测任务，需允许目标端口 (80/443) 的出站请求 |
| 磁盘空间 | >= 200 MB | 用于存放代码、依赖包及 SQLite 数据库文件 |
| 内存 | >= 512 MB | 推荐最低空闲内存，保障探测任务并发执行时的稳定性 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | /docs/user-guide/ | 如何添加资源、配置探测间隔、查看状态历史、导出配置 |
| 运维参考 | /docs/operations/ | 如何部署至生产环境、配置反向代理、调整数据库连接池 |
| API 文档 | /docs/api/ | 如何调用 RESTful 接口获取资源列表、触发手动探测、订阅 Webhook |
| 开发指南 | /docs/development/ | 如何扩展探测协议、新增标签插件、自定义前端主题 |

## 资源列表

### 实时比分数据源

<code>dianjingbifenw.org.cn</code>

<code>bifenzhibow.org.cn</code>

<code>bifenzaixianw.net.cn</code>

### 在线比分与历史归档

<code>bifenzaixianw.org.cn</code>

<code>bifenwangw.org.cn</code>

### 综合比分导航

<code>bifenwangbf.net.cn</code>

<code>bifen500w.org.cn</code>

## 项目结构

```
bifenhub/
├── src/                                # 核心源代码目录
│   ├── server/                         # 服务端逻辑 (Express 应用、路由、中间件)
│   │   ├── index.js                    # 服务入口，负责启动 HTTP 与探测调度器
│   │   ├── routes/                     # RESTful API 路由定义 (资源、标签、探测)
│   │   └── workers/                    # 后台任务进程 (可用性探测、变更检测)
│   ├── client/                         # 前端界面源码 (React + Vite)
│   │   ├── pages/                      # 页面级组件 (看板、设置、详情、历史)
│   │   ├── components/                 # 可复用 UI 组件 (卡片、标签、表格、图表)
│   │   └── hooks/                      # 自定义 React Hooks (数据请求、轮询、本地存储)
│   ├── core/                           # 核心业务逻辑 (资源管理、标签引擎、比对算法)
│   │   ├── sourceManager.js            # 资源增删改查与元数据规范化
│   │   ├── probeEngine.js              # 多协议探测引擎 (HTTP/HTTPS 超时、重试、证书解析)
│   │   └── diffChecker.js              # 响应摘要计算与历史版本比对
│   └── lib/                            # 通用工具函数 (日志、配置解析、加密、日期格式化)
├── data/                               # 数据存储目录
│   ├── sources.json                    # 默认资源列表 (JSON 格式，首次启动加载)
│   └── cache/                          # 离线缓存目录 (存储静态说明文档与示例)
├── config/                             # 环境配置文件
│   ├── default.yaml                    # 默认配置 (端口、探测间隔、超时阈值)
│   └── production.yaml                 # 生产环境覆盖配置 (日志级别、数据库路径)
├── docs/                               # 文档目录 (用户手册、运维参考、API 文档、开发指南)
├── tests/                              # 单元测试与集成测试 (Jest + Supertest)
├── scripts/                            # 构建与运维辅助脚本 (数据库初始化、数据迁移)
├── package.json                        # npm 项目清单 (依赖声明与脚本命令)
├── README.md                           # 项目说明文档 (即本文档)
└── LICENSE                             # MIT 许可证文件
```

## 贡献指南

1. 复刻项目仓库并创建功能分支。请从 main 分支签出 feature/ 或 fix/ 前缀的新分支，确保分支命名清晰反映改动内容，例如 feature/probe-timeout-config 或 fix/source-save-error。

2. 编写或更新单元测试。所有新增或修改的核心逻辑必须附带对应的单元测试用例，测试覆盖率不得低于 85%。提交前请运行 npm test 确保全部测试通过且无回归问题。

3. 提交 Pull Request 前更新文档。若您的改动涉及用户可见的功能变化、配置项变更或 API 行为调整，请同步更新 /docs 目录下的对应文档，并在 PR 描述中标注文档变更位置。

4. 签署开发者贡献者许可协议。首次提交 PR 时需在 PR 评论中明确声明同意本项目的贡献者许可协议条款，协议文本可在 CONTRIBUTING.md 中查看。

5. 遵循代码风格规范。项目统一使用 ESLint + Prettier 进行代码格式化，提交前请运行 npm run lint 与 npm run format 自动修复格式问题，确保代码风格与主分支保持一致。

## 常见问题

**问：可用性探测任务是否会频繁触发目标源的反爬策略？**

答：项目默认探测间隔为 5 分钟，每次探测仅发送单次 GET 请求并携带模拟主流浏览器的 User-Agent 头，不执行 JavaScript 解析或自动重试风暴。若目标源存在严格访问频率限制，建议在配置文件中将 probe.interval 调整为 30 分钟或更长，并启用 probe.randomDelay 选项以分散请求时间点，降低触发风控规则的概率。

**问：如何迁移现有资源列表至另一台服务器？**

答：在源服务器的管理界面中进入 设置 -> 导出 模块，选择 JSON 格式导出，下载生成的配置文件。在目标服务器上启动 BifenHub 后，进入同一模块选择 导入，上传该文件即可完成资源列表、标签体系及自定义探测规则的完整迁移。若需同时迁移探测历史数据，请额外备份 data/history.db 文件并替换目标服务器的对应文件。

**问：是否支持同时监测不同协议类型的数据源？**

答：支持。除 HTTP/HTTPS 协议外，项目还内置了对 DNS 解析可用性、TCP 端口连通性以及 Ping 回包延迟的基础探测能力。在添加或编辑资源时，可通过 protocol 字段指定协议类型（http / https / dns / tcp / icmp），并根据协议类型填写对应的 target 字段格式（如域名、IP:端口 或 纯域名）。具体协议参数说明请参考 /docs/user-guide/protocol-support.md。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:44:17
