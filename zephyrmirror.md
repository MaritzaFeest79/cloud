# RizhiLink Aggregator

RizhiLink Aggregator 是一个面向中文互联网技术内容创作者、数据分析师与信息聚合站点的外链资源导航与实时数据看板项目。项目定位为轻量级、可自部署的技术资源目录站，专注于解决技术博客、直播数据平台、赛事信息站点以及排行类网站在内容外链管理、资源分类展示与动态数据引用中的结构化组织问题。目标用户包括独立开发者、小型技术团队、内容运营人员以及开源社区贡献者，帮助其快速构建具备清晰导航层级与外部资源引用能力的聚合页面，降低重复开发成本，提升信息获取效率。

本项目不提供具体数据存储或后端业务逻辑，而是作为前端资源映射与路由引导层，通过静态配置方式将分散的外部链接按业务语义进行分组、标注与展示。项目内置默认资源列表，涵盖直播数据、赛事预测、排行榜单等常见技术内容外链类型，用户可直接使用或根据自身需求增删修改。项目代码完全开源，遵循 MIT 许可证，鼓励社区贡献新的资源分类规则与展示模板。

## 功能概览

- **外链资源分组管理** 支持按业务类别（如直播、赛事、排行）对 URL 进行分组展示，每组可独立设置标题与描述，便于用户快速定位所需内容。

- **静态配置化数据源** 所有外链资源以 JSON / YAML 配置文件形式存储，无需数据库，修改即生效，适合快速迭代与版本管理。

- **响应式网格布局** 前端采用 CSS Grid / Flexbox 实现自适应卡片布局，在桌面端、平板与移动设备上均能保持良好的浏览体验。

- **实时状态占位钩子** 预留状态检测接口钩子，用户可扩展实现外链可用性监控、响应时间标注或更新日期提示，目前以静态占位展示。

- **标签过滤与搜索** 内置简单的标签系统与前端关键词搜索功能，支持按标签筛选资源或通过标题关键词模糊匹配，提升海量链接下的查找效率。

- **暗色主题支持** 默认提供亮色与暗色两套 CSS 变量主题，根据系统偏好自动切换，也可手动指定，减轻长时间阅读时的视觉疲劳。

- **可嵌入组件化设计** 核心资源列表与卡片组件可独立引用为 Web Component 或 iframe 嵌入其他页面，方便在多站点间复用同一份资源目录。

## 应用场景

- **技术博客的外链导航页** 个人技术博主或团队博客可使用本项目构建独立的“友情链接”或“常用工具”页面，将日常参考的直播数据平台、技术文档站、趋势排行榜等统一收纳，提升读者访问便利性。

- **小型赛事数据看板辅助** 对于关注直播赛事或排行榜动态的小型社区，本项目可作为数据入口聚合页，将分散在不同域名的实时比分、赛程预测、选手榜单等链接集中展示，减少用户在多个站点间切换的成本。

- **内部团队资源目录** 开发团队或研究小组可利用本项目的分组与搜索能力，搭建内部使用的技术资源仓库，存放 API 文档、监控面板、日志平台等内部链接，并通过配置文件进行版本化维护。

- **开源项目示例站点** 作为开源项目自身的示例部署，展示如何通过纯前端方式组织大量外链，并为其他开发者提供可复用的目录结构与样式模板，降低类似项目的启动门槛。

## 快速开始

以下步骤假定您已安装 Git 与 Node.js（推荐 LTS 版本），并希望在本机运行开发服务器。

```bash
# 克隆项目仓库
git clone https://github.com/rizhilink/aggregator.git rizhilink-aggregator

# 进入项目目录
cd rizhilink-aggregator

# 安装依赖（使用 npm 或 yarn）
npm install

# 启动开发服务器，默认监听 3000 端口
npm run dev
```

启动成功后，在浏览器中访问 `http://localhost:3000` 即可查看默认资源列表页面。若需构建生产版本，请执行 `npm run build`，产物将输出至 `dist` 目录，可部署至任何静态托管服务。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | >= 18.0.0 | 运行环境与包管理基础，推荐使用 LTS 版本 |
| npm 或 yarn | >= 8.0.0 / >= 1.22.0 | 依赖安装与脚本执行工具，任选其一 |
| 现代浏览器 | 最近两个版本（Chrome / Firefox / Edge / Safari） | 前端运行环境，需支持 ES Module 与 CSS Grid |
| Git | >= 2.25.0 | 用于克隆仓库与版本管理（非运行必需，但推荐） |
| 静态托管服务 | 任意（如 Nginx、Vercel、Netlify、OSS） | 生产部署依赖，仅需支持 HTML / CSS / JS 静态文件 |
| 可选：VS Code | 最新版 | 推荐编辑器，含 ESLint / Prettier 插件支持 |
| 可选：Docker | >= 20.10 | 若使用容器化部署方式，需安装 Docker 引擎 |

## 文档导航

| 层面 | 目录 / 资源 | 回答的问题 |
|------|------------|-----------|
| 用户指南 | `/docs/user-guide.md` | 如何配置资源列表、自定义分组、修改主题颜色与启用搜索功能？ |
| 开发者文档 | `/docs/developer-guide.md` | 项目目录结构、核心组件 API、样式变量说明以及如何扩展新的卡片类型？ |
| 部署手册 | `/docs/deployment.md` | 如何将项目部署到 Vercel、Netlify、Nginx 或 Docker 容器中？ |
| 贡献规范 | `/CONTRIBUTING.md` | 提交 Issue / PR 的流程、代码风格检查规则以及 Commit Message 格式要求？ |
| 示例配置 | `/examples/sample-config.json` | 完整的资源配置文件示例，涵盖所有可配置字段与数据结构说明？ |
| 常见问题 | `/docs/faq.md` | 开发或部署过程中遇到的典型问题解决方案汇总？ |

## 资源列表

本节按业务类别收录项目默认引用的全部外部资源链接，所有 URL 均保留用户提供的原始格式，未做任何协议补全、域名规范化或路径修改。

### 直播数据类

- <code>rizhilianzhibo.asia</code>

- <code>rizhiliantuijian.asia</code>

### 赛事信息类

- <code>rizhiliansheshoubang.asia</code>

- <code>rizhiliansaicheng.asia</code>

- <code>rizhilianqianzhan.asia</code>

### 实时数据类

- <code>rizhilianjishibifen.asia</code>

### 排行榜单类

- <code>rizhilianjifenbang.asia</code>

## 项目结构

项目采用模块化组织方式，核心源码位于 `src` 目录下，配置文件与构建脚本位于根目录。以下为关键目录与文件的说明：

```
rizhilink-aggregator/
├── src/                            # 核心源代码目录
│   ├── components/                 # 可复用 UI 组件
│   │   ├── ResourceCard.vue        # 资源卡片组件，展示单个链接的标题、描述与标签
│   │   ├── ResourceGrid.vue        # 网格布局容器，管理卡片排列与响应式断点
│   │   └── SearchBar.vue           # 搜索框组件，支持关键词过滤与清空
│   ├── config/                     # 配置相关模块
│   │   ├── resources.json          # 默认外链资源数据（含分组、标签、标题等字段）
│   │   └── theme.js                # 主题变量与切换逻辑（亮色 / 暗色）
│   ├── styles/                     # 全局样式与 CSS 变量
│   │   ├── variables.css           # 颜色、间距、字体等设计令牌
│   │   ├── reset.css               # 浏览器默认样式重置
│   │   └── grid.css                # 网格系统与辅助类样式
│   ├── utils/                      # 工具函数集合
│   │   ├── filter.js               # 基于关键词与标签的过滤逻辑
│   │   ├── sort.js                 # 按标题或更新时间排序的辅助函数
│   │   └── storage.js              # 本地存储读写（主题偏好、搜索历史）
│   ├── App.js                      # 根组件，负责整体布局与状态管理
│   └── main.js                     # 应用入口，初始化 Vue 实例并挂载
├── public/                         # 静态资源目录
│   ├── favicon.ico                 # 站点图标
│   └── robots.txt                  # 搜索引擎爬虫规则（默认允许全部）
├── docs/                           # 项目文档（用户指南、开发者文档、部署手册等）
├── tests/                          # 单元测试与集成测试脚本
│   ├── unit/                       # 组件与工具函数的单元测试
│   └── e2e/                        # 端到端测试用例（使用 Playwright）
├── .eslintrc.js                    # ESLint 代码风格检查配置
├── .prettierrc                     # Prettier 代码格式化配置
├── vite.config.js                  # Vite 构建工具配置（含代理、别名、插件）
├── package.json                    # 项目依赖与脚本定义
├── README.md                       # 项目说明文档（即本文档）
└── LICENSE                         # MIT 许可证文本
```

## 贡献指南

我们欢迎并鼓励社区贡献者以多种方式参与本项目，包括但不限于提交新资源分类规则、改进前端样式、完善文档或报告问题。请遵循以下步骤：

1.  **查阅现有 Issue 与 Project** 访问 GitHub Issues 页面，查看当前待解决的问题或功能请求。若您发现重复或已关闭的议题，请勿重复提交，可在已有议题下补充评论。

2.  **Fork 仓库并创建功能分支** 将本项目 Fork 至您的个人账号，然后基于 `main` 分支创建新的功能分支，分支命名建议使用 `feature/功能简述` 或 `fix/问题简述` 格式。

3.  **编写代码并确保通过检查** 在本地完成代码修改后，运行 `npm run lint` 检查代码风格，运行 `npm run test` 确保单元测试通过。新增功能需附带对应的测试用例。

4.  **提交 Pull Request** 推送分支至您的 Fork 仓库，然后向本项目的 `main` 分支发起 Pull Request。请在 PR 描述中清晰说明改动内容、关联 Issue 编号以及测试覆盖情况。

5.  **参与 Code Review** PR 提交后，维护者将进行代码审查。请及时响应反馈意见，必要时补充修改。合并后您的贡献将出现在下一版本的更新日志中。

## 常见问题

**问：如何修改默认显示的资源列表？**

答：资源数据位于 `src/config/resources.json` 文件中，您可以直接编辑该文件，按照已有的字段结构（如 `group`、`title`、`url`、`tags`、`description`）增删或修改条目。修改保存后，开发服务器会自动热重载，刷新页面即可看到更新。若需彻底更换数据源，您也可以替换该文件或修改 `App.js` 中的导入路径。

**问：项目是否支持后端 API 动态获取资源？**

答：当前版本为纯静态前端，所有资源在构建时编译进 JS 包中。若需要从后端 API 获取，您可以在 `src/utils/` 下新增 `fetch.js` 工具函数，并在 `App.js` 的 `mounted` 生命周期中调用，替换掉默认的静态导入。我们计划在后续版本中提供可选的 API 适配层，但不会强制依赖后端。

**问：生产部署后搜索功能无法正常工作，可能是什么原因？**

答：搜索功能完全在浏览器端执行，依赖于 `resources.json` 中的数据。请检查部署后的静态文件是否包含了完整的资源数据（可打开浏览器开发者工具的 Network 面板，查看 `main.js` 或 `resources.json` 是否成功加载）。若使用路由模式（如 History 模式），请确保服务器已配置正确的回退规则（fallback），否则刷新非根路径可能导致资源加载失败。建议优先使用 Hash 模式部署。

## 许可证

本项目采用 MIT 许可证。您可以自由使用、修改、分发本项目的源代码，包括商业用途，但需保留原始版权声明与许可声明。详细条款请参阅项目根目录下的 `LICENSE` 文件。

> 外链数量: 7 | 生成时间: 2026-08-11 03:44:18
