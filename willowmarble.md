# NexusIndex

NexusIndex 是一个面向技术社区与开源生态的高密度外链资源聚合系统，专为开发者、技术写作者与项目维护者设计，用于高效组织、分类、校验和展示外部参考链接。它不是一个简单的链接收藏工具，而是一个可定制的技术元数据索引层，能够将散落于文档、议题、PR 和教程中的关键外部资源统一收敛至可追溯、可审查、可版本化的结构化目录中。

本项目适用于需要长期维护大量外部引用链接的开源项目文档站、技术博客聚合页、知识库导航系统，以及任何希望提升外链管理透明度与可维护性的工程团队。NexusIndex 本身不存储任何第三方内容，仅提供链接的元信息描述与状态监控能力，帮助用户在文档驱动开发过程中降低链接失效、域名劫持或内容迁移带来的信息腐化风险。

## 功能概览

- **批量链接导入与清洗**：支持从 CSV、JSON 或纯文本列表批量导入原始 URL，自动执行协议补全、尾部斜杠规范化、域名降级与无效字符过滤，确保入库链接符合基本可访问性标准。

- **分类标签与多级目录绑定**：允许为每条链接分配一个主分类和多个辅助标签，并可将其绑定至项目文档中的特定章节或页面路径，实现链接与上下文的强关联。

- **定时可用性探测**：内置基于 Head 请求的轻量级健康检查器，可配置周期（每小时/每日/每周）对全部或指定链接集合发送探测请求，记录响应状态码、响应时间和重定向链，并生成状态变更报告。

- **状态变更告警与摘要邮件**：当链接状态从可用变为不可用，或发生永久重定向（301/308）时，系统可通过 Webhook 或 SMTP 邮件向维护者发送变更摘要，便于及时更新或替换失效资源。

- **公开状态徽章嵌入**：为每个链接分类或整体集合生成 SVG 格式的状态徽章（如"健康率 98%"、"失效链接 3"），可嵌入至 README、文档页脚或仪表盘，增强链接集合的透明度。

- **RESTful API 与只读视图**：提供 JSON API 供内部或第三方工具查询链接状态与元信息；同时支持生成纯只读的 HTML 表格视图，适合作为独立导航页面部署。

- **历史变更审计日志**：所有链接的添加、删除、属性修改以及状态变更均记录审计日志，支持按时间范围回溯，便于定位某篇文档发布时的外部依赖状态。

## 应用场景

1. **开源项目文档站的外链治理**：大型开源项目的文档通常引用大量外部规范、教程、API 参考和依赖库主页。NexusIndex 可帮助文档维护者定期扫描这些链接的有效性，在版本发布前生成链接健康报告，避免用户点击到已失效的参考资源。

2. **技术博客聚合与专栏导航**：技术社区或团队博客常维护"精品文章推荐"或"外部工具列表"页面。使用 NexusIndex 管理这些链接后，可以自动展示每条链接的最后检查时间与状态，让读者对资源可用性一目了然。

3. **知识库内部的资源依赖映射**：企业或开源组织的内部知识库（如 Wiki、Confluence、GitBook）中散落着大量指向内部系统、SaaS 服务或云控制台的链接。NexusIndex 可作为独立旁路服务，定期验证这些链接的可达性，并在内部系统发生域名变更时快速定位受影响页面。

4. **学术或技术资源导航站的后端支撑**：面向特定领域（如编程语言、框架、数据库、算法）的资源导航网站，往往需要维护数百条外部链接。NexusIndex 的分类、标签和状态探测能力可显著降低手动维护成本，同时为站点提供实时状态数据。

## 快速开始

以下步骤将在本地环境启动 NexusIndex 实例，使用 SQLite 作为默认存储后端，并加载示例链接集合。

```bash
# 克隆项目仓库
git clone https://github.com/nexusindex/nexusindex.git
cd nexusindex

# 安装 Python 依赖（推荐使用虚拟环境）
python -m venv venv
source venv/bin/activate  # Windows 下使用 venv\Scripts\activate
pip install -r requirements.txt

# 初始化数据库并创建默认配置
python scripts/init_db.py
python scripts/seed_sample_links.py

# 启动开发服务器（默认监听 127.0.0.1:5000）
python app.py
```

启动后，访问 `http://127.0.0.1:5000/dashboard` 可查看链接总览面板。首次运行将自动触发一次全量可用性探测，耗时取决于链接数量与网络状况。

## 安装要求

| 依赖 | 必需版本 | 说明 |
|------|----------|------|
| Python | 3.9 - 3.11 | 核心运行环境，3.12 暂未完成兼容性测试 |
| SQLite | 3.35 及以上 | 内置数据库，用于元数据和审计日志存储，无需额外安装 |
| requests | 2.28.0 及以上 | 用于发送 HTTP 探测请求，支持超时与重试策略 |
| click | 8.1.0 及以上 | 命令行工具框架，用于管理脚本和定时任务 |
| PyYAML | 6.0 及以上 | 用于解析配置文件（links.yaml 和 settings.yaml） |
| APScheduler | 3.10.0 及以上 | 提供后台定时调度能力，用于周期性健康检查 |
| Flask | 2.2.0 及以上 | 仅当启用 Web 仪表盘或 API 服务时需要 |
| email-validator | 2.0.0 及以上 | 用于邮件告警模块中的邮箱地址校验，可选依赖 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户指南 | `docs/user-guide/` | 如何导入链接、配置分类标签、设置探测周期与告警规则？ |
| 运维手册 | `docs/operations/` | 如何部署至生产环境、配置反向代理、调整数据库连接池与日志级别？ |
| API 参考 | `docs/api/` | RESTful API 的端点列表、请求参数格式、认证方式与返回结构？ |
| 架构设计 | `docs/architecture/` | 系统模块划分、数据流走向、扩展点设计以及插件化加载机制？ |

## 资源列表

本项目的初始示例链接集合来源于公开网络资源，用于演示分类标签与状态探测功能。所有链接按所属领域归类如下。

### 综合参考类

- <code>bifenguanwang.org.cn</code>
- <code>ruidianchaobisaijieguo.org.cn</code>

### 赛事数据类

- <code>ajiabifen.org.cn</code>
- <code>ajiabisaijieguo.org.cn</code>
- <code>ruidianchaobifen.org.cn</code>

### 专项结果类

- <code>danchaobisaijieguo.org.cn</code>
- <code>nuochaobisaijieguo.org.cn</code>

## 项目结构

```
nexusindex/
├── app/                                # 主应用模块
│   ├── __init__.py                     # Flask 应用工厂与配置加载
│   ├── routes/                         # 路由层（仪表盘、API、状态视图）
│   │   ├── dashboard.py                # 管理面板与统计概览
│   │   ├── api_v1.py                   # RESTful API 端点实现
│   │   └── badge.py                    # SVG 状态徽章生成
│   ├── models/                         # 数据模型与 ORM 映射
│   │   ├── link.py                     # 链接实体（URL、分类、标签、添加时间）
│   │   ├── check_record.py             # 探测记录（状态码、响应时间、重定向链）
│   │   └── audit_log.py                # 审计日志（操作者、操作类型、变更内容）
│   ├── services/                       # 业务逻辑服务层
│   │   ├── checker.py                  # 并发探测调度器与健康检查引擎
│   │   ├── importer.py                 # 批量导入处理器（CSV/JSON/YAML 解析）
│   │   └── notifier.py                 # 告警通知服务（Webhook / SMTP）
│   └── utils/                          # 工具函数集合
│       ├── url_cleaner.py              # URL 规范化与域名提取
│       ├── validators.py               # 输入校验与白名单过滤
│       └── time_utils.py               # 时间戳转换与周期计算
├── scripts/                            # 运维与开发辅助脚本
│   ├── init_db.py                      # 数据库初始化与表结构创建
│   ├── seed_sample_links.py            # 注入示例链接（资源列表中的 7 条）
│   └── run_health_check.py             # 手动触发全量健康检查
├── config/                             # 配置文件目录
│   ├── settings.yaml                   # 全局配置（探测超时、重试次数、调度周期）
│   └── links.yaml                      # 链接主数据（分类、标签、备注）
├── tests/                              # 单元测试与集成测试
│   ├── test_checker.py                 # 探测引擎的模拟网络测试
│   ├── test_importer.py                # 导入器对畸形输入的处理测试
│   └── test_api.py                     # API 端点的响应结构与状态码测试
├── docs/                               # 完整文档（见上方文档导航）
├── logs/                               # 运行日志与审计日志存储目录（自动创建）
├── requirements.txt                    # 生产环境依赖锁定
├── requirements-dev.txt                # 开发与测试额外依赖
└── README.md                           # 本文件
```

## 贡献指南

1. 在 GitHub 上 fork 本仓库，并在本地检出 `main` 分支的最新代码。建议新建一个描述性功能分支，例如 `feature/add-https-probing` 或 `fix/import-csv-encoding`。

2. 运行 `pip install -r requirements-dev.txt` 安装开发依赖（包含 pytest、black、flake8）。所有代码提交前请执行 `black .` 与 `flake8` 确保格式与规范通过。

3. 若新增或修改链接处理逻辑（如清洗规则、探测策略），请同步更新 `tests/` 下对应的单元测试用例，并确保全部测试通过（执行 `pytest`）。

4. 提交 PR 时，请在描述中清晰说明变更动机、涉及模块、手动测试步骤以及是否影响现有配置兼容性。若变更涉及数据库迁移，请附带迁移脚本与回滚说明。

5. 文档更新与代码变更同等重要。若新增 API 端点或配置项，请同步更新 `docs/` 下的对应手册。翻译类贡献请确保中英文术语一致。

## 常见问题

**问：探测检查会消耗大量网络带宽或触发目标站点的反爬策略吗？**

答：NexusIndex 默认使用 HEAD 请求进行探测，仅获取响应头信息，不下载响应体。单次探测超时设置为 5 秒，重试次数为 1 次。对于大规模链接集合（超过 1000 条），建议将探测周期设置为每日一次，并启用请求间隔抖动（jitter）以避免集中请求。您也可以在配置文件中自定义每个域名的请求间隔或黑名单。

**问：链接状态显示为 "不可用"，但我在浏览器中可以正常访问，为什么？**

答：可能存在以下情况：1) 目标站点对 HEAD 请求返回 405 或 403，但允许 GET 请求，此时可在配置中为特定域名启用 GET 探测模式；2) 目标站点启用了 TLS 指纹校验或 Cloudflare 人机验证，此类环境无法通过自动化请求模拟，建议将该链接标记为 "需手动验证" 并跳过自动探测；3) 网络环境差异（如 IPv6 与 IPv4、DNS 解析不一致），可尝试在探测配置中指定 DNS 服务器。

**问：如何将现有的大量链接迁移至 NexusIndex，而不丢失已有的分类信息？**

答：项目提供了 `scripts/import_bulk.py` 脚本，支持读取包含 `url`、`category`、`tags`、`description` 列的 CSV 或 JSON 文件。您可以将现有链接按该格式整理后一次性导入。导入过程会执行去重检查（基于规范化后的 URL），并生成导入报告，列出跳过的重复项与格式错误的条目。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
