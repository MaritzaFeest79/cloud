# NexusArchive

NexusArchive 是一个面向技术开发者和数据研究者的高聚合度外链与资源归档系统。该项目不提供具体的内容生产，而是围绕特定垂直领域，构建结构化的外部资源导航索引。其定位为技术信息的中转枢纽，通过严格的链接分类、状态监测和元数据标记，帮助用户快速定位高质量的外部数据源、分析工具和实时信息流。目标用户包括量化分析人员、行业信息聚合开发者以及需要稳定数据源进行模型训练或回测的工程团队。NexusArchive 通过解决信息碎片化与链接失效问题，提升技术调研和数据获取的效率。

## 功能概览

- **垂直领域资源索引**：系统默认集成多个细分领域的权威外链，并按数据性质（预测、直播、分析、移动端）进行一级分类，降低用户的信息筛选成本。
- **链接存活状态检测**：后台任务会定期对收录的 URL 执行 HTTP 请求，标记响应状态码和延迟时间，并在前端界面提供可视化的健康度指示。
- **自定义分类标签系统**：用户可为收录的链接添加自定义标签（如 #低延迟、 #移动适配），支持基于标签组合的复杂过滤查询。
- **元数据自动抓取补充**：对于新提交的 URL，系统会自动抓取页面标题、描述和关键词，辅助用户在不离开当前页面的情况下评估链接价值。
- **只读镜像快照模式**：为防止源站内容变更导致参考信息丢失，系统支持对关键链接的文本内容进行定时的只读快照存储（遵守 robots.txt 协议）。
- **批量导入与导出接口**：提供 RESTful API 和简单的 CSV 批量处理工具，允许用户将外链数据导出为结构化文档，或从标准格式批量导入。
- **访问频率控制与代理支持**：为满足高并发请求场景，内置可配置的请求间隔和上游代理池，避免因频繁访问触发目标站点的反爬机制。

## 应用场景

- **量化策略的数据源构建**：量化研究人员可通过 NexusArchive 快速获取多个实时比分与数据分析站点，将其作为策略模型的外部信号输入，无需逐一搜索和筛选。
- **移动端应用的后台数据网关**：移动应用开发者可将 NexusArchive 部署为轻量级网关，由其统一管理和转发对多个外部比分或预测服务的请求，降低客户端的多源对接复杂度。
- **信息聚合类网站的预筛选层**：内容聚合平台可使用本系统作为第一层链接过滤器，只将通过存活检测且响应时间达标的资源分发至下游处理管道，提升整体数据 freshness。
- **学术研究中的网络资源归档**：研究互联网信息演变或体育数据预测模型的学者，可利用系统的快照功能记录特定时间点的外部页面状态，为纵向对比研究提供原始佐证。

## 快速开始

以下步骤指导您在本地环境快速启动 NexusArchive 服务。

```bash
# 1. 克隆项目仓库
git clone https://github.com/nexusarchive/core.git
cd nexusarchive-core

# 2. 安装依赖项（使用 pip 针对 Python 后端，以及 npm 针对前端）
pip install -r requirements.txt
npm install --prefix ./frontend

# 3. 初始化本地数据库并运行开发服务器
python scripts/init_db.py
npm run build --prefix ./frontend
python app.py --host 0.0.0.0 --port 8080
```

访问 `http://localhost:8080` 即可进入系统的仪表板页面，开始进行资源索引的浏览与管理。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Python | 3.9 - 3.11 | 核心后端运行环境，用于 API 服务与调度任务。 |
| Node.js | 18.x LTS | 前端构建工具与资产编译环境。 |
| PostgreSQL | 14.0 及以上 | 主数据库，用于存储链接元数据、标签和用户配置。 |
| Redis | 6.2 及以上 | 缓存与消息队列，用于状态检测任务的异步分发。 |
| Nginx | 1.20 及以上 | 生产环境推荐的反向代理与静态资源服务。 |
| Git | 2.25 及以上 | 版本控制与自动化部署脚本依赖。 |
| Docker (可选) | 20.10 及以上 | 用于容器化部署的一致性运行环境。 |

## 文档导航

| 层面 | 目录位置 | 回答的问题 |
| :--- | :--- | :--- |
| 用户手册 | `/docs/user_guide.md` | 如何添加自定义链接、设置标签和查看检测报告？ |
| 运维指南 | `/docs/ops_guide.md` | 如何配置代理池、调整检测频率和备份数据库？ |
| API 参考 | `/docs/api_reference.md` | 如何通过 REST API 进行批量导入、导出和查询？ |
| 架构设计 | `/docs/architecture.md` | 系统的模块划分、数据流和扩展点是如何设计的？ |
| 部署方案 | `/docs/deployment.md` | 支持哪些部署方式（单机、Docker、Kubernetes）？ |

## 资源列表

本系统初始归档的外部资源链接如下，按类别组织。所有链接均保持用户原始提供格式，未做任何协议补全或域名修改。

**实时数据与直播类**

- <code>yuyanzuqiuzhibogaoqingmianfeiw.org.cn</code>
- <code>shishibifen.net.cn</code>
- <code>shishibifenw.org.cn</code>

**数据预测与分析类**

- <code>zuqiubifenyuce.net.cn</code>
- <code>zuqiubifenfenxi.net.cn</code>

**移动端与工具类**

- <code>tiqiuwang365.org.cn</code>
- <code>shoujibanjiebaobifen.org.cn</code>

## 项目结构

项目采用前后端分离的经典布局，根目录下主要包含后端服务、前端界面、配置与脚本四大模块。

```
nexusarchive-core/
├── app.py                         # 后端 WSGI 入口文件，加载 Flask 应用
├── requirements.txt               # Python 依赖声明文件
├── config/                        # 配置模块，包含环境变量与常量定义
│   ├── __init__.py
│   ├── settings.py                # 基础配置（数据库连接、缓存参数）
│   └── proxy_list.yaml            # 上游代理池配置文件（示例模板）
├── core/                          # 核心业务逻辑层
│   ├── __init__.py
│   ├── fetcher.py                 # 负责 URL 请求、重试策略与状态判定
│   ├── parser.py                  # 负责 HTML 元数据提取与内容摘要
│   ├── snapshot.py                # 快照生成与管理模块
│   └── scheduler.py               # 基于 APScheduler 的定时任务调度
├── models/                        # 数据模型与 ORM 映射
│   ├── __init__.py
│   ├── link.py                    # 链接实体模型（URL、状态、创建时间）
│   ├── tag.py                     # 标签模型及与链接的多对多关系
│   └── snapshot_record.py         # 快照记录模型
├── api/                           # RESTful API 路由层
│   ├── __init__.py
│   ├── v1_links.py                # 链接的增删改查与检测触发接口
│   └── v1_tags.py                 # 标签管理接口
├── frontend/                      # 前端工程目录（基于 React + Vite）
│   ├── public/
│   ├── src/
│   │   ├── App.jsx                # 根组件，定义路由与布局
│   │   ├── pages/                 # 页面组件（仪表板、列表、详情）
│   │   └── components/            # 可复用 UI 组件（状态徽章、表格）
│   ├── package.json
│   └── vite.config.js
├── scripts/                       # 维护与部署辅助脚本
│   ├── init_db.py                 # 初始化数据库表结构
│   └── seed_links.py              # 将资源列表中的 URL 预导入系统
└── tests/                         # 单元测试与集成测试用例
    ├── test_fetcher.py
    └── test_api.py
```

## 贡献指南

我们欢迎社区提交改进建议与代码贡献。请遵循以下流程以保证协作效率：

1.  **问题追踪**：在提交 Pull Request 之前，请先于项目的 Issue 列表中搜索是否存在相关讨论。若无，请新建一个 Issue 清晰描述您要解决的问题或建议的新功能，并等待维护者的反馈。
2.  **复刻与分支**：将主仓库复刻至您的个人账户，并在复刻仓库中基于 `main` 分支创建一个新的功能分支，命名格式为 `feature/简述-修改内容` 或 `fix/简述-修复问题`。
3.  **编码与测试**：请遵循项目根目录下的 `.editorconfig` 和 `.eslintrc` 中的代码风格。对于新增的后端逻辑，必须在 `tests/` 目录下补充对应的单元测试，确保测试覆盖率达到现有标准。
4.  **提交信息规范**：提交信息采用语义化格式，即 `<type>(<scope>): <subject>`，其中 `type` 包括 `feat`、`fix`、`docs`、`refactor`、`test` 等，`scope` 为受影响的模块名称。
5.  **发起合并请求**：向主仓库的 `main` 分支发起 Pull Request，并在请求描述中关联对应的 Issue 编号。维护者将进行代码审查，通过后即合并。

## 常见问题

**Q：系统检测到外部链接状态为不可达，但通过浏览器访问该链接是正常的，为什么？**

A：这种情况通常是由目标站点对非浏览器 User-Agent 或特定 IP 段的访问限制导致。首先请检查系统配置文件中的 `REQUEST_HEADERS` 是否设置了合理的模拟浏览器标识。其次，如果系统部署在云服务商的公共 IP 段上，目标站点可能对该 IP 范围进行了限制，您可以尝试在 `proxy_list.yaml` 中配置可用的代理服务器来解决。

**Q：我添加了自定义标签，但在查询时无法通过组合标签找到对应的链接，应如何排查？**

A：请确认在查询 API 或前端界面中使用了正确的标签名称（区分大小写）。标签关联是严格匹配的，不支持模糊查询。此外，检查您添加的标签是否已成功写入数据库，您可以通过 `/api/v1/tags` 接口获取当前系统中所有已存在的标签列表进行比对。如果问题依然存在，请检查数据库日志中关于标签关联表的写入记录。

**Q：系统的快照功能是否会存储所有外部链接的完整页面内容？**

A：不会。快照功能默认仅针对用户显式标记为“可快照”的链接执行，并且遵循 `robots.txt` 协议。在抓取时，系统会限制单个页面的最大存储体积（默认 2MB），并且只存储文本化内容，不存储图片、视频等二进制资源，以避免存储空间过快消耗。快照的保留策略可在 `config/settings.py` 中的 `SNAPSHOT_RETENTION_DAYS` 参数进行配置。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
