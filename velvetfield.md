# HyperLink Navigator

HyperLink Navigator 是一个轻量化、高可用的技术资源导航与外部链接聚合系统，专为开发者、技术团队及内容运营人员设计，用于高效管理、分类展示和快速跳转高频使用的技术文档、社区论坛、内部工具面板与第三方数据看板。该项目不存储任何用户数据或业务内容，仅作为链接路由与展示层，通过静态站点生成或容器化部署，在数分钟内即可搭建起一个结构清晰、可维护的外部资源门户，从而解决团队内部资源分散、书签冗余、新成员上手成本高等实际问题。

HyperLink Navigator 的核心定位是一个“链接治理工具”，而非普通书签管理器。它强调链接的生命周期管理，包括链接的添加、失效检测、分类归档、访问统计与权限标识。项目内置轻量级缓存机制，可配置链接健康检查策略，并生成简单的可用性报表。同时，项目完全遵守用户提供的 URL 原始格式，不对任何外部链接进行协议补全、域名规范化或路径改写，确保跳转行为与用户预期严格一致。

## 功能概览

- **多级分类导航体系**：支持无限层级的目录结构，便于按业务领域、技术栈或使用频率对链接进行组织，每个分类可独立设置图标与描述。
- **链接生命周期管理**：提供链接的添加、归档、删除和标记失效功能，支持设置链接的生效时间范围与访问权限级别。
- **自动健康检查**：后台定时任务对已收录链接进行 HTTP/HTTPS 可达性探测，自动标记异常链接并生成告警通知，减少无效跳转。
- **访问统计与热度排序**：记录每个链接的点击次数与最后访问时间，支持按热度、新增时间或字母顺序对列表进行动态排序。
- **快速搜索与过滤**：基于标题、描述、分类标签和 URL 关键词的实时搜索，支持按分类和状态（正常/失效/待审核）过滤结果。
- **链接导入与导出**：支持从标准书签 HTML 文件、CSV 或 JSON 格式批量导入链接数据，并支持导出为 Markdown 表格或 JSON 结构化数据。
- **响应式布局与深色模式**：前端界面适配桌面与移动设备，内置浅色/深色主题切换，提升不同环境下的浏览体验。

## 应用场景

1.  **技术团队内部文档聚合**：开发团队可将日常使用的 API 文档、设计稿链接、CI/CD 流水线地址、日志平台和监控看板统一纳入 HyperLink Navigator，新成员加入时只需访问一个入口即可获取所有必要资源，极大缩短初始化配置时间。
2.  **开源项目外部依赖索引**：开源项目维护者可以在项目 README 中引用 HyperLink Navigator 构建的链接列表，集中管理所有依赖库、参考实现、社区论坛和版本发布地址，避免 README 正文过于冗长。
3.  **数据看板与报表门户**：数据分析团队可将各类实时数据大屏、周报生成器、数据导出工具和第三方数据服务商的入口集中展示，配合健康检查功能，确保所有数据源地址有效可用。
4.  **多环境配置管理**：运维人员可利用该导航系统管理开发、测试、预发布和生产环境的不同控制台地址，通过分类标签清晰区分环境，降低误操作风险。

## 快速开始

以下步骤将指导您在本地环境快速启动 HyperLink Navigator 开发实例。

```bash
# 1. 克隆项目仓库
git clone https://github.com/hyperlink-navigator/hyperlink-navigator.git
cd hyperlink-navigator

# 2. 安装项目依赖
npm install

# 3. 启动开发服务器
npm run dev
```

执行上述命令后，开发服务器将在默认端口 3000 启动。访问 `http://localhost:3000` 即可看到导航主页。如需构建生产版本，请使用 `npm run build` 命令。

## 安装要求

HyperLink Navigator 基于 Node.js 和 React 构建，部署前请确保环境满足以下要求。

| 依赖项 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Node.js | 18.17.0 或更高 | 运行时环境，推荐使用 LTS 版本 |
| npm | 9.0.0 或更高 | 包管理器，用于安装依赖和执行脚本 |
| SQLite | 3.0.0 或更高 | 嵌入式数据库，用于存储链接与分类数据，无需额外安装 |
| Git | 2.30.0 或更高 | 用于克隆仓库和版本管理 |
| Docker | 20.10.0 或更高 | 仅容器化部署需要，本地开发可忽略 |
| Nginx | 1.20.0 或更高 | 生产环境反向代理建议，非强制依赖 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
| :--- | :--- | :--- |
| 用户指南 | `/docs/user-guide/` | 如何添加、编辑、删除链接；如何管理分类；如何使用搜索和排序功能；如何导入导出书签数据 |
| 管理员手册 | `/docs/admin-manual/` | 如何配置健康检查间隔；如何设置访问权限；如何查看链接可用性报表；如何备份和恢复数据库 |
| 开发指南 | `/docs/developer-guide/` | 项目目录结构与模块划分；如何扩展新的链接字段；如何自定义前端主题；如何编写单元测试 |
| 部署运维 | `/docs/deployment/` | 如何使用 Docker 镜像部署；如何配置 Nginx 反向代理；如何启用 HTTPS；如何设置环境变量 |

## 资源列表

本章节按类别收录本批次全部外部资源链接。所有 URL 均严格遵循用户原始输入格式，未做任何修改或规范化处理。

**体育数据与直播类**
- <code>500bifenzhibo.asia</code>
- <code>500bifenwang.asia</code>
- <code>500bifen.asia</code>
- <code>puchaojifenbang.asia</code>
- <code>xijiaguanwang.asia</code>

**足球赛事信息类**
- <code>agentingzuqiujiajiliansaijifenbang.site</code>
- <code>agentingzuqiujiajiliansaisaicheng.site</code>

## 项目结构

```
hyperlink-navigator/
├── backend/                        # 后端服务代码
│   ├── src/                        # 核心源码目录
│   │   ├── controllers/            # 路由控制器，处理链接与分类的 CRUD 操作
│   │   ├── models/                 # 数据模型定义，基于 Sequelize ORM 操作 SQLite
│   │   ├── services/               # 业务逻辑层，包含健康检查、统计计算等核心服务
│   │   ├── middlewares/            # 请求中间件，包含身份验证、日志记录与错误处理
│   │   ├── routes/                 # RESTful API 路由定义，对应 /api/v1 前缀
│   │   ├── utils/                  # 通用工具函数，包括 URL 格式化、时间处理与加密
│   │   └── app.js                  # 应用入口，初始化 Express 服务器
│   ├── tests/                      # 后端单元测试与集成测试用例
│   ├── package.json                # 后端依赖管理文件
│   └── Dockerfile                  # 后端容器化构建文件
├── frontend/                       # 前端界面代码
│   ├── src/                        # 前端源码目录
│   │   ├── components/             # React 组件库，包括导航树、链接卡片、搜索栏等
│   │   ├── pages/                  # 页面级组件，包含主页、分类页、管理后台页
│   │   ├── hooks/                  # 自定义 React Hooks，用于数据请求与状态管理
│   │   ├── store/                  # Redux 状态管理模块，处理应用全局状态
│   │   ├── styles/                 # 全局样式文件，包含主题变量与响应式设计
│   │   ├── utils/                  # 前端工具函数，包含请求封装与数据格式化
│   │   └── index.js                # 前端入口文件，渲染 React 根组件
│   ├── public/                     # 静态资源目录，包含 favicon 与 robots.txt
│   ├── package.json                # 前端依赖管理文件
│   └── Dockerfile                  # 前端构建与静态文件服务镜像配置
├── config/                         # 全局配置文件目录
│   ├── default.yaml                # 默认配置，包含端口、数据库路径与日志级别
│   ├── production.yaml             # 生产环境覆盖配置
│   └── development.yaml            # 开发环境覆盖配置
├── scripts/                        # 运维与辅助脚本
│   ├── health-check.js             # 独立运行的链接健康检查脚本，可配合 cron 调度
│   ├── data-migration.js           # 数据库迁移与种子数据填充脚本
│   └── backup-db.sh               # SQLite 数据库备份脚本
├── docs/                           # 项目文档目录，内容涵盖用户指南、API 参考等
├── docker-compose.yml              # 容器编排文件，用于一键启动前后端与数据库
├── README.md                       # 项目主说明文档，即本文档
├── LICENSE                         # MIT 许可证文件
├── .gitignore                      # Git 忽略规则文件
└── package.json                    # 根目录包文件，用于管理整体项目脚本
```

## 贡献指南

我们欢迎并感谢任何形式的贡献。请按照以下步骤参与本项目开发：

1.  **提交问题报告或功能建议**：请在 GitHub Issues 页面搜索是否已存在类似问题，若无则新建一个 Issue，详细描述您遇到的问题或希望增加的功能，并附上必要的环境信息与复现步骤。
2.  **派生项目并创建功能分支**：点击项目页面右上角的 Fork 按钮创建个人副本，然后将该副本克隆至本地。请基于 `main` 分支创建一个新的功能分支，分支命名应反映其用途，例如 `feature/add-export-csv` 或 `fix/health-check-timeout`。
3.  **编写代码并添加测试**：在您的分支上进行代码修改，请确保代码风格与项目现有规范保持一致。对于新增功能或修复，请编写相应的单元测试或集成测试，确保测试通过。
4.  **提交变更并发起拉取请求**：提交代码时请撰写清晰、简洁的提交信息。将您的分支推送到 GitHub 上的派生仓库，然后向主仓库的 `main` 分支发起 Pull Request。请在 PR 描述中关联相关的 Issue 编号，并简要说明您的改动内容与测试结果。

## 常见问题

**问：项目必须使用 SQLite 数据库吗？可以更换为 MySQL 或 PostgreSQL 吗？**

答：项目默认配置使用 SQLite 是为了降低部署门槛，无需额外安装数据库服务。如果您需要更换为 MySQL 或 PostgreSQL，请修改 `config/` 目录下的配置文件，将 `dialect` 字段改为对应的数据库类型，并安装相应的 Node.js 驱动包。请注意，更换数据库后需要手动执行建表脚本或使用项目提供的迁移工具初始化数据结构。

**问：如何配置链接健康检查的时间间隔和告警方式？**

答：健康检查的间隔时间（默认为每小时）可通过环境变量 `HEALTH_CHECK_INTERVAL` 或配置文件的 `health.interval` 字段进行调整，单位支持分钟和小时（例如 `"30m"` 或 `"2h"`）。告警通知当前支持输出到控制台日志和写入数据库的告警表，如需发送邮件或 Webhook，您需要继承 `services/NotifierService` 类并实现自定义通知逻辑，然后在配置文件中指定使用的通知器。

**问：前端页面可以自定义品牌 Logo 和标题吗？**

答：可以。请编辑 `frontend/public/` 目录下的 `favicon.ico` 和 `logo.png` 文件替换为您的品牌标识。页面标题和副标题可在 `frontend/src/config/site.config.js` 文件中修改 `siteTitle` 和 `siteDescription` 字段。所有自定义配置均不会影响核心功能，且支持通过环境变量动态注入。

## 许可证

本项目采用 MIT 许可证。MIT 许可证是一种宽松的开源许可证，允许任何人免费获取、使用、修改、合并、发布、分发、再许可和/或出售本软件的副本，前提是在所有副本或实质性部分中包含上述版权声明和许可声明。本软件按“现状”提供，不提供任何明示或暗示的保证，包括但不限于适销性、特定用途适用性和非侵权性的保证。有关完整条款，请参阅项目根目录下的 LICENSE 文件。

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
