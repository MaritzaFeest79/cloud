# WebLink Navigator

WebLink Navigator 是一个面向开发人员和技术研究者的轻量级外链资源聚合与导航系统。该项目定位于将分散在多个技术社区、文档站点和工具平台中的高质量外部链接进行集中管理、分类展示与健康状态监控，解决开发者在日常工作中频繁查找重复资源、遗忘常用工具入口、以及无法快速判断外部服务可用性等实际问题。WebLink Navigator 不存储任何用户数据，不提供内容代理或缓存，仅作为结构化链接索引层，帮助团队和个人建立清晰的外链资源地图。

## 功能概览

- **多级分类导航**：支持按技术领域、服务类型、使用频率等维度对链接进行动态分类，便于快速定位目标资源。

- **链接健康检查**：定期对收录的外部链接执行 HTTP/HTTPS 可达性探测，自动标记异常链接并生成告警日志。

- **模糊搜索与标签筛选**：内置基于关键词和预设标签的快速过滤机制，支持在数百条链接中秒级定位匹配项。

- **导入导出标准化**：支持通过 JSON 或 YAML 格式批量导入链接列表，同时支持将当前分类结构导出为静态 HTML 导航页。

- **访问统计看板**：记录各链接在系统内的点击频次和最后访问时间，辅助判断资源热度与使用趋势。

- **自定义分类树**：允许管理员通过配置文件自由增删改查分类层级，无需修改核心代码即可适配不同业务场景。

- **响应式布局**：前端展示层适配桌面端与移动端浏览器，确保在多种屏幕尺寸下均具备良好可读性。

## 应用场景

- **技术团队内部知识库入口聚合**：开发团队可将常用的 API 文档、组件库首页、CI/CD 控制台、日志查询平台等分散链接统一收录至 WebLink Navigator，新成员入职时仅需访问导航首页即可获得全部必要资源入口，大幅降低环境熟悉成本。

- **个人开发环境起始页**：独立开发者或研究员可将每日必用的代码托管平台、在线工具、技术社区、数据源网站等配置为个人导航页，配合浏览器启动页设置，实现打开浏览器即直达工作上下文。

- **项目文档外链附录管理**：开源项目或企业内部项目在编写技术文档时，可将涉及的外部参考链接（如依赖库主页、协议规范、参考实现等）通过 WebLink Navigator 集中维护，并在 README 中仅保留导航入口，避免文档正文被大量外部链接淹没。

- **服务依赖链路可视化辅助**：运维或 SRE 人员可利用链接健康检查功能，将核心外部服务（如容器镜像仓库、Helm Chart 源、配置中心地址等）纳入监控，当某个依赖链接不可达时快速定位故障范围。

## 快速开始

```bash
# 克隆项目仓库
git clone https://github.com/weblink-navigator/weblink-navigator.git

# 进入项目目录
cd weblink-navigator

# 安装依赖（基于 Node.js 22 LTS）
npm install

# 以开发模式启动本地服务（默认端口 3000）
npm run dev
```

启动后，在浏览器中访问 <code>http://localhost:3000</code> 即可查看默认导航页面。首次启动会自动载入内置示例链接分类数据，用户可通过编辑 `data/links.json` 文件替换为自定义链接列表。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | 22.x LTS 或更高 | 运行时环境，用于执行服务端代码与构建脚本 |
| npm | 10.x 或更高 | 包管理器，用于安装项目依赖 |
| SQLite | 3.40 或更高（内置） | 嵌入式数据库，用于存储链接元数据与访问日志 |
| Git | 2.40 或更高 | 版本控制工具，用于克隆仓库及后续更新 |
| 现代浏览器 | Chrome 110+ / Firefox 110+ / Edge 110+ | 前端界面运行环境，需支持 ES2022 标准 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|-----|------|-----------|
| 用户指南 | docs/user-guide/ | 如何添加链接、创建分类、设置标签、查看统计与健康报告 |
| 管理员手册 | docs/admin-guide/ | 如何配置健康检查间隔、调整导入导出格式、备份数据文件 |
| 开发文档 | docs/developer-guide/ | 项目模块划分、API 接口定义、前端组件结构及二次开发流程 |
| 部署参考 | docs/deployment/ | 支持 Docker 部署、systemd 服务配置、Nginx 反向代理示例 |

## 资源列表

本导航系统预置了以下技术资源链接作为示例数据，用户可根据实际需求增删改查。所有链接均为原始提供形式，系统不做任何内容代理或修改。

技术社区与资讯

- <code>jishibifen500gw.org.cn</code>

篮球赛事数据

- <code>hupuzuqiujishibifen.org.cn</code>
- <code>hupuzuqiubisaijieguo.org.cn</code>
- <code>hupuzuqiubifenwang.org.cn</code>
- <code>hupuzuqiubifensaicheng.org.cn</code>
- <code>hupuzuqiubifen.org.cn</code>

综合体育数据

- <code>hasakechaosaicheng.org.cn</code>

## 项目结构

```
weblink-navigator/
├── src/                           # 源代码主目录
│   ├── server/                    # 服务端代码 (Node.js + Express)
│   │   ├── index.js               # 入口文件，启动 HTTP 服务与数据库连接
│   │   ├── routes/                # 路由定义 (链接管理、分类、健康检查等)
│   │   ├── controllers/           # 请求处理器，包含参数校验与响应格式化
│   │   └── services/              # 业务逻辑层 (链接增删改查、导入导出、统计计算)
│   ├── client/                    # 前端代码 (React + Tailwind)
│   │   ├── pages/                 # 页面级组件 (首页、分类视图、管理面板)
│   │   ├── components/            # 可复用 UI 组件 (链接卡片、搜索框、筛选器)
│   │   ├── hooks/                 # 自定义 React Hooks (数据请求、本地存储)
│   │   └── styles/                # 全局样式与 Tailwind 配置
│   ├── shared/                    # 前后端共享代码
│   │   ├── types/                 # TypeScript 类型定义 (链接对象、分类结构)
│   │   └── validators/            # 数据校验规则 (链接格式、分类名称)
│   └── db/                        # 数据库相关
│       ├── migrations/            # SQLite 表结构变更脚本
│       └── seed/                  # 初始示例数据 (含预置链接与分类)
├── data/                          # 运行时数据目录 (用户自定义链接、日志)
│   ├── links.json                 # 可手动编辑的链接导入导出文件
│   └── health-logs/               # 健康检查结果历史记录
├── tests/                         # 单元测试与集成测试
│   ├── unit/                      # 服务端与工具函数单元测试
│   └── e2e/                       # 端到端测试 (使用 Playwright)
├── docs/                          # 完整文档 (用户指南、开发文档、部署手册)
├── scripts/                       # 辅助工具脚本 (数据迁移、健康检查触发)
├── config/                        # 环境配置文件 (开发、测试、生产)
│   ├── default.yaml               # 默认配置 (端口、数据库路径、检查间隔)
│   └── custom.yaml.example        # 自定义配置示例 (用户可复制后修改)
├── .eslintrc.js                   # JavaScript/TypeScript 代码规范配置
├── .prettierrc                    # 代码格式化配置
├── package.json                   # npm 项目声明与依赖列表
├── Dockerfile                     # 容器化构建文件 (基于 Alpine Linux)
├── docker-compose.yml             # 本地开发与测试用容器编排
└── README.md                      # 项目总体说明文档 (当前文件)
```

## 贡献指南

1.  **问题报告与需求提议**：在提交代码变更之前，建议先在 GitHub Issues 中描述您遇到的问题或希望增加的功能，并附带复现步骤或使用场景说明。项目维护者会在 48 小时内给予初步反馈。

2.  **分支开发流程**：请基于 `main` 分支创建新的特性分支，分支命名建议遵循 `feature/功能简述` 或 `fix/问题简述` 格式。所有开发工作在该分支上完成，并确保提交日志清晰可读。

3.  **本地测试与代码检查**：提交前需在本地执行 `npm run test` 确保所有单元测试通过，并运行 `npm run lint` 检查代码风格是否符合项目 ESLint 配置。对于前端 UI 变更，建议附带简要的截图或录屏说明。

4.  **发起 Pull Request**：将开发分支推送至远程仓库后，向 `main` 分支发起 Pull Request。PR 描述中需说明变更目的、影响范围以及是否包含破坏性修改。至少需要一名项目维护者审核通过后方可合并。

## 常见问题

**问：WebLink Navigator 是否支持对外的在线演示站点？**

答：项目本身不提供公共托管实例，但您可以通过执行 `npm run build` 构建生产版本，并使用 `npm run start` 在本地或自有服务器上运行。如需快速体验，可使用 `docker-compose up` 启动容器化实例，访问 <code>http://localhost:3000</code> 查看默认界面。

**问：如何批量导入我自己的链接列表，而不是使用内置示例数据？**

答：您可以直接编辑 `data/links.json` 文件，按照项目提供的 JSON Schema 格式填写链接对象（包含标题、URL、分类、标签等字段），保存后重启服务即可生效。此外，也支持通过管理界面右上角的“导入”按钮上传 YAML 或 JSON 文件完成批量导入，导入操作会覆盖当前数据库中的链接数据。

**问：链接健康检查会对目标站点造成压力吗？**

答：健康检查采用 HEAD 请求方法，仅获取响应头信息而不下载完整页面内容，每次请求数据量极小。检查间隔默认配置为每 24 小时执行一次，且仅对系统内收录的链接发起请求，不会产生频繁或大量并发访问。您也可以在 `config/default.yaml` 中调整检查间隔或禁用该功能。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:44:19
