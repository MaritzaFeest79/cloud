# 500 Resource Hub

500 Resource Hub 是一个面向数据分析师、运维工程师与技术决策者的高性能外链资源聚合平台。项目定位于将分散在多个垂直领域的实时数据面板、分析工具与预测模型入口进行集中化管理，通过统一的导航层降低信息获取延迟，提升团队在赛事数据、趋势预测与历史对照场景下的工作效率。目标用户包括数据中台团队、量化策略开发者以及需要快速切换多个外部数据源的技术人员。

项目本身不存储任何敏感数据，仅作为 URL 路由与状态管理层，解决多源切换中的书签分散、跨域策略不一致、访问时效性难以监控等实际问题。通过轻量级响应式前端与健康检查机制，确保每个外链在纳入导航前均通过可用性验证，从而为内部系统提供可靠的数据源网关。

## 功能概览

- **多源统一看板**：将七个核心数据面板入口集中呈现，支持单页快速跳转，减少标签页混乱。
- **实时状态监控**：每个外链均配有被动健康检查标识，超时或不可达时自动置灰并记录日志。
- **分组筛选视图**：按分析类型、数据粒度、更新频率等维度对资源进行标签化分类。
- **快速搜索定位**：基于资源标题与描述的关键词模糊匹配，支持键盘快捷键唤起。
- **自定义排序与收藏**：用户可将高频使用的资源置顶，并保存个人排序偏好至本地存储。
- **访问统计看板**：记录各资源点击频次与平均响应时间，辅助团队评估数据源质量。
- **深色模式适配**：完整支持系统级深色主题切换，降低夜间操作视觉疲劳。

## 应用场景

- **实时赛事数据聚合**：数据分析团队可在同一页面内交替查阅最新预测、比分变动与历史拆局数据，用于构建短期趋势模型。
- **运营监控大屏辅助**：运维人员将本平台作为大屏数据源的前置路由，一键切换不同分析面板，避免手动输入长域名。
- **策略回测对照**：量化策略开发者通过并列打开多个历史比分与拆局资源，快速比对不同时间段的数据特征，验证因子有效性。
- **新人培训导航**：团队新成员通过本平台了解公司常用的外部数据源体系，减少熟悉环境的时间成本。

## 快速开始

以下命令将项目克隆至本地，安装依赖并启动开发服务器。

```bash
# 克隆仓库
git clone https://github.com/500hub/resource-hub.git
cd resource-hub

# 安装依赖（使用 npm 或 yarn）
npm install

# 启动开发服务（默认监听 3000 端口）
npm run dev
```

启动后，在浏览器中访问 `http://localhost:3000` 即可看到资源导航界面。所有外链数据均配置在 `config/sources.json` 中，可直接编辑该文件进行增删改。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | >= 18.0.0 | 运行时环境，用于构建与开发服务器 |
| npm | >= 9.0.0 或 yarn >= 1.22 | 包管理器，用于安装项目依赖 |
| Git | >= 2.30.0 | 版本控制，用于克隆仓库及贡献管理 |
| 现代浏览器 | Chrome 90+ / Firefox 88+ / Edge 90+ | 前端运行时，需支持 ES2021 及 CSS Grid |
| 网络连通性 | 可访问公网 DNS | 用于外链健康检查与资源加载 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户指南 | `docs/user-guide.md` | 如何添加自定义资源、如何切换视图模式 |
| 运维手册 | `docs/ops-manual.md` | 健康检查机制、日志位置、故障排查流程 |
| 开发规范 | `docs/dev-spec.md` | 代码风格、提交信息格式、PR 流程 |
| 架构设计 | `docs/architecture.md` | 前端路由设计、状态管理、缓存策略 |
| 部署指引 | `docs/deployment.md` | 生产环境构建、Nginx 配置、CDN 对接 |

## 资源列表

以下列表包含本平台当前托管的所有外部资源链接，按类别分组。每个链接均保持用户原始输入格式，不做任何协议补全或域名改写。

### 实时预测类

- <code>500zuixinyuce.asia</code>
- <code>500zuixinfenxi.asia</code>
- <code>500jinrituijian.asia</code>

### 实时比分类

- <code>500zuqiubifen.asia</code>
- <code>500wanchangbifen.asia</code>

### 历史与拆局类

- <code>500jiubanbifen.asia</code>
- <code>500tuijian.asia</code>

## 项目结构

```
500-resource-hub/
├── public/                         # 静态资源目录
│   ├── favicon.ico                 # 站点图标
│   └── robots.txt                  # 爬虫规则
├── src/                            # 源代码主目录
│   ├── assets/                     # 图片、字体等静态资产
│   │   ├── logos/                  # 平台 Logo 变体
│   │   └── themes/                 # 深色/浅色主题变量
│   ├── components/                 # 可复用 UI 组件
│   │   ├── Header/                 # 顶部导航栏组件
│   │   ├── ResourceCard/           # 单个资源卡片渲染
│   │   ├── SearchBar/              # 搜索输入及快捷键处理
│   │   ├── FilterGroup/            # 分类筛选标签组
│   │   └── HealthBadge/            # 健康状态指示器
│   ├── config/                     # 配置文件目录
│   │   ├── sources.json            # 核心外链数据源列表（含分组与描述）
│   │   └── categories.json         # 分类映射与显示顺序
│   ├── hooks/                      # 自定义 React Hooks
│   │   ├── useHealthCheck.ts       # 外链可用性轮询逻辑
│   │   └── useLocalStorage.ts      # 用户偏好持久化
│   ├── pages/                      # 路由页面
│   │   ├── Home.tsx                # 主看板页面
│   │   └── Settings.tsx            # 用户自定义配置页
│   ├── services/                   # 外部接口服务层
│   │   ├── pingService.ts          # 轻量级 HTTP HEAD 探测
│   │   └── logService.ts           # 点击行为日志收集
│   ├── styles/                     # 全局样式与主题
│   │   ├── globals.css             # 基础重置与字体
│   │   └── theme.css               # CSS 变量主题系统
│   ├── types/                      # TypeScript 类型定义
│   │   ├── resource.d.ts           # Resource 对象接口
│   │   └── config.d.ts             # 配置结构类型
│   └── utils/                      # 工具函数
│       ├── sortResources.ts        # 自定义排序算法
│       └── urlValidator.ts         # URL 格式校验与归一化辅助
├── tests/                          # 单元测试与集成测试
│   ├── components/                 # 组件快照测试
│   └── services/                   # 服务层模拟测试
├── docs/                           # 完整文档目录（见文档导航）
├── .github/                        # GitHub 工作流配置
│   └── workflows/                  # CI/CD 流水线
│       ├── build.yml               # 构建验证
│       └── health-check.yml        # 定时外链健康扫描
├── .eslintrc.js                    # ESLint 代码规范
├── .prettierrc                     # Prettier 格式化配置
├── index.html                      # 入口 HTML 模板
├── package.json                    # 项目依赖与脚本定义
├── tsconfig.json                   # TypeScript 编译配置
├── vite.config.ts                  # Vite 构建工具配置
└── README.md                       # 本文件
```

## 贡献指南

1. **问题反馈**：在 GitHub Issues 中使用 `bug` 或 `enhancement` 模板描述问题，请附上浏览器版本与复现步骤。
2. **分支开发**：从 `main` 分支切出 `feature/` 或 `fix/` 前缀的分支，保持单次提交聚焦单一修改点。
3. **代码规范**：提交前运行 `npm run lint` 与 `npm run test`，确保所有检查通过，测试覆盖不低于 80%。
4. **资源更新**：若需增删外链，直接修改 `src/config/sources.json`，并在 PR 描述中说明数据源用途与可用性依据。
5. **文档同步**：任何功能变更需同步更新 `docs/` 下对应文档，并确保 README 中的快速开始步骤仍然可执行。

## 常见问题

**Q：部分外链显示为不可用（灰色），但我手动访问是正常的。**  
A：健康检查基于 HTTP HEAD 请求，某些站点可能拒绝 HEAD 方法或响应较慢。您可点击卡片上的刷新按钮手动重试，或调整 `src/config/sources.json` 中对应条目的 `timeout` 字段（单位毫秒）。若持续异常，请提交 Issue 说明站点响应特性。

**Q：我可以将这个平台部署到内网，并只使用内网数据源吗？**  
A：完全可以。您只需修改 `sources.json` 中的 URL 列表为内网地址，并移除或替换健康检查的探测端点即可。项目本身无任何强制外网依赖，构建后为纯静态文件。

**Q：如何备份我的个人排序和收藏配置？**  
A：所有用户偏好均存储在浏览器本地存储（LocalStorage）中，键名为 `500hub_preferences`。您可通过浏览器开发者工具导出该键值，或点击设置页面的“导出配置”按钮生成 JSON 文件。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
