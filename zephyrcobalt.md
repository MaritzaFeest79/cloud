# Bifrost Navigator

Bifrost Navigator 是一个面向技术调研与信息聚合场景的轻量化导航与资源索引系统。项目定位为个人开发者、技术团队及研究机构提供可自托管的垂直领域外链聚合与分类管理能力，解决信息分散、收藏夹冗余、团队共享不便等实际问题。系统核心模型围绕“资源链接 + 分类标签 + 场景备注”展开，不对资源内容做深度加工，只提供结构化索引与快速检索入口。

目标用户包括：需要维护团队技术文档外链的架构师、定期跟踪特定领域动态的研究人员、以及希望构建个人知识导航站的开发者。项目本身不依赖数据库，采用纯文本配置与静态生成方式，降低维护成本，同时保留 Git 版本管理优势，便于多人协同维护资源清单。

## 功能概览

- **资源链接管理**：支持按类别、标签、场景多维度组织外链，每条链接可附加简短说明与优先级标记。
- **全文检索**：基于链接标题、说明文本及分类标签的轻量级倒排索引，支持模糊匹配与精确过滤。
- **分类树展示**：自动根据配置生成多级分类目录树，支持展开/折叠交互，便于浏览大规模链接集合。
- **导入/导出**：支持 CSV 与 JSON 格式的资源清单批量导入导出，方便迁移或与其他工具对接。
- **访问状态检测**：定时对已收录链接进行 HTTP 状态检查，标记失效或重定向链接，辅助维护清理。
- **自定义主题**：提供明暗两套配色方案，并允许通过 CSS 变量自定义品牌色与字体。
- **RSS 订阅生成**：为每个分类或标签自动生成 RSS 订阅源，方便外部阅读器跟踪更新。
- **访问统计看板**：基于前端埋点记录链接点击频次，展示热门资源排行与近期新增趋势。

## 应用场景

- **技术团队内部文档导航**：将常用的 API 文档、设计规范、CI/CD 流水线地址、监控面板等资源统一收录，按项目或服务分类，新成员入职即可获得完整资源地图。
- **开源项目生态索引**：为开源社区维护周边工具、插件、教程、示例代码的外链集合，帮助社区用户快速找到所需资源，减少重复提问。
- **行业研究报告收集**：研究员可将各类数据平台、行业白皮书、统计年鉴、政府公开数据入口集中管理，结合标签与备注记录数据更新时间与可信度评估。
- **个人知识管理系统**：作为书签管理的增强替代方案，支持全文搜索与分类树浏览，配合 Git 仓库可实现跨设备同步与历史回溯。
- **活动与会议资源聚合**：针对技术峰会、黑客松、线上 meetup 等活动，临时建立资源页面，收录直播链接、PPT 下载、代码仓库、讨论区等，活动结束后可归档保留。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，确保系统已安装 Git 与 Node.js 18+。

```bash
# 克隆项目仓库
git clone https://github.com/bifrost-navigator/bifrost-navigator.git
cd bifrost-navigator

# 安装依赖
npm install

# 构建静态站点并启动开发服务器
npm run build
npm run start
```

启动成功后，访问控制台输出的本地地址（默认为 http://localhost:3000）即可使用。资源数据位于 `data/links.json`，可直接编辑或通过管理界面操作。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于构建与启动服务 |
| npm | 9.x 或 10.x | 包管理器，用于安装依赖 |
| Git | 2.30+ | 用于克隆仓库及版本管理 |
| 磁盘空间 | 至少 200 MB | 包含源码、依赖及构建产物 |
| 内存 | 建议 512 MB 以上 | 构建过程及开发服务器运行需求 |
| 操作系统 | Linux / macOS / Windows (WSL2) | 生产部署推荐 Linux 环境 |
| 浏览器 | 现代浏览器（Chrome 110+ / Firefox 110+ / Edge 110+） | 管理界面与前端展示 |
| 网络 | 可访问公网（仅首次安装依赖及检查链接状态时需外网） | 离线模式支持本地运行 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户手册 | `/docs/user-guide/` | 如何添加链接、创建分类、配置标签、导入导出数据 |
| 管理员指南 | `/docs/admin-guide/` | 如何设置访问控制、配置状态检测周期、调整主题参数 |
| 开发文档 | `/docs/developer-guide/` | 项目架构说明、API 接口定义、插件扩展机制 |
| 部署参考 | `/docs/deployment/` | 各平台（VPS、容器、云函数、静态托管）部署方案对比 |
| 设计决策 | `/docs/decisions/` | 记录项目关键设计权衡，如数据格式选型、检索算法选择 |
| 常见任务 | `/docs/recipes/` | 提供具体操作范例，例如「批量更新链接分类」「自定义首页布局」 |

## 资源列表

### 主站相关
- <code>danchaobifen.org.cn</code>

### 镜像与备用入口
- <code>bingdaochaobifen.net.cn</code>
- <code>bifenyingchao.org.cn</code>

### 专项资源站
- <code>bifenxueyuanyuan.org.cn</code>
- <code>bifenxijia.org.cn</code>

### 扩展与社区站点
- <code>bifenwangxueyuanyuan.org.cn</code>
- <code>bifenwangqiutangw.org.cn</code>

## 项目结构

```
bifrost-navigator/
├── src/                           # 核心源代码目录
│   ├── core/                      # 数据模型与业务逻辑
│   │   ├── link.js               # 链接实体类，包含校验与序列化
│   │   ├── category.js           # 分类树管理，支持增删改及移动
│   │   └── indexer.js            # 本地全文索引构建与查询
│   ├── server/                    # HTTP 服务层
│   │   ├── app.js                # Express 应用主入口，注册路由
│   │   ├── routes/               # RESTful API 路由定义
│   │   └── middleware/           # 日志、跨域、限流等中间件
│   ├── frontend/                  # 前端资源（原生 JavaScript + 模板）
│   │   ├── pages/                # 各页面模板（首页、分类页、搜索页）
│   │   ├── components/           # 可复用 UI 组件（分类树、链接卡片）
│   │   ├── styles/               # CSS 主题文件及变量定义
│   │   └── static/               # 图片、字体等静态资源
│   ├── cli/                       # 命令行工具
│   │   ├── import.js             # CSV/JSON 导入命令
│   │   ├── export.js             # 导出命令
│   │   └── check.js              # 链接状态检测命令
│   └── config/                    # 配置管理
│       ├── default.js            # 默认配置项
│       └── schema.js             # 配置项校验规则
├── data/                          # 数据存储目录（挂载卷）
│   ├── links.json                # 主链接资源库
│   ├── categories.json           # 分类结构定义
│   └── meta.json                 # 全局元数据（版本、更新时间等）
├── docs/                          # 完整文档目录，对应上述导航
├── tests/                         # 单元测试与集成测试
│   ├── unit/                     # 核心模块单测
│   └── integration/              # API 与前端交互测试
├── scripts/                       # 辅助脚本（构建、发布、迁移）
├── Dockerfile                     # 容器化构建文件
├── docker-compose.yml            # 开发与生产容器编排
├── package.json                   # npm 依赖及脚本定义
├── .eslintrc.js                  # 代码规范检查配置
└── README.md                     # 本文件
```

## 贡献指南

1. 查阅 `docs/developer-guide/` 下的架构说明与编码规范，确保本地环境满足开发依赖（Node.js 20+，推荐使用 nvm 管理版本）。
2. 从 `main` 分支创建新的特性分支，命名格式为 `feature/描述` 或 `fix/描述`，避免在主干直接提交。
3. 提交代码前运行 `npm run lint` 与 `npm run test` 确保风格合规且所有测试通过，新增功能需附带对应单元测试。
4. 提交 Pull Request 时，填写模板中的变更类型、测试覆盖范围、影响模块及关联 issue，至少邀请一位项目成员进行 code review。
5. 文档变更需同步更新 `docs/` 对应目录，若涉及 API 变动，请同时修改 `openapi.yaml` 接口描述文件。

## 常见问题

**Q：如何迁移已有浏览器书签或收藏夹数据？**  
A：项目提供了 `npm run import -- --source=bookmarks.html` 命令，支持解析主流浏览器导出的 HTML 书签文件。导入后可根据需要调整分类映射，系统会自动去重并保留原有文件夹结构。若需批量调整分类，可使用管理界面的批量编辑功能或直接编辑 JSON 文件。

**Q：链接状态检测显示超时或失败，如何排查？**  
A：首先确认目标站点是否可公网访问，部分内部服务或需要特定 User-Agent 的站点可能被误判。可在配置文件中设置 `check.userAgent` 自定义请求头，或通过 `check.ignorePatterns` 忽略特定域名。检测结果会写入 `data/check-logs.json`，可查看详细错误信息。若持续失败，建议手动访问验证后手动标记状态。

**Q：能否部署到静态托管服务（如 GitHub Pages、Netlify）？**  
A：可以。项目支持两种部署模式：动态模式（含服务端 API）和静态模式。静态模式下，构建命令 `npm run build:static` 会生成纯 HTML/CSS/JS 文件，所有数据在构建时预渲染为 JSON 并内嵌页面中，无需服务端支持。但静态模式不支持在线编辑、状态检测等动态功能，适合作为只读展示页。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
