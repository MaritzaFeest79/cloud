# NavSphere

NavSphere 是一个面向技术团队与个人开发者的轻量级导航与资源聚合平台，旨在解决多源技术文档、工具站点与社区入口分散、难以统一管理的问题。项目定位于为中小型研发组织、开源社区及独立开发者提供可自托管的导航门户，通过结构化分类与简洁交互，降低信息检索成本，提升日常研发效率。NavSphere 本身不存储任何外部资源内容，仅作为合规链接的索引与展示层，所有外链均以原始形式呈现，确保数据透明性与可审计性。

## 功能概览

- **分类导航目录**：支持无限层级的自定义分类体系，便于按技术栈、业务领域或使用频率组织链接集合。
- **链接健康检测**：定时对收录的 URL 进行可达性探测，自动标记异常链接并生成告警日志。
- **全文检索与标签过滤**：基于标题、描述和自定义标签进行模糊搜索，支持多标签组合筛选。
- **导入与导出机制**：支持批量导入 JSON/CSV 格式链接数据，并导出为 Markdown 表格或结构化备份文件。
- **访问统计面板**：记录各链接的点击次数与最近访问时间，辅助分析高频资源与冷门条目。
- **响应式布局与暗色模式**：内置两套主题，适配桌面与移动端浏览，满足不同光照环境下的使用习惯。
- **权限分级管理**：区分管理员、编辑与访客角色，控制链接增删改查及分类调整的操作权限。

## 应用场景

1. **研发团队内部文档聚合**：将分散在 Confluence、GitLab Wiki、语雀和飞书文档中的常用入口统一归入 NavSphere，新成员入职时可快速查阅核心指引。
2. **开源项目生态页搭建**：开源维护者可将依赖库、社区论坛、CI/CD 状态页和版本发布记录整合为项目导航，放置于 GitHub README 或组织官网。
3. **技术学习路线整理**：学习者可按前端、后端、运维、算法等方向整理视频课程、官方文档和练习平台，形成个人知识索引。
4. **多环境测试入口管理**：测试团队可将各环境（开发、测试、预发布、生产）的运维面板和监控图表集中展示，避免频繁切换浏览器书签。
5. **行业资讯与数据站点订阅**：分析师或研究员可将行业报告源、数据可视化工具和实时指标看板收归一处，便于日常调研与数据核对。

## 快速开始

以下步骤适用于 Linux/macOS 及 Windows WSL 环境，要求已安装 Git 和 Node.js 18+。

```bash
# 克隆项目仓库
git clone https://github.com/navsphere/navsphere.git
cd navsphere

# 安装依赖（使用 npm）
npm install

# 配置环境变量（复制示例配置）
cp .env.example .env

# 初始化本地数据库（SQLite）
npm run db:init

# 启动开发服务器（默认监听 3000 端口）
npm run dev
```

启动后访问 `http://localhost:3000` 即可进入导航管理界面。首次使用需根据引导创建管理员账户。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | 18.x 或 20.x LTS | 运行时环境，推荐使用官方预编译二进制或 nvm 管理 |
| npm | 9.x 或 10.x | 随 Node 一并安装，用于包管理与脚本执行 |
| SQLite | 3.35+ | 嵌入式数据库，用于存储链接、分类及访问日志 |
| Git | 2.30+ | 用于克隆仓库及后续拉取更新 |
| 操作系统 | Linux (glibc 2.28+), macOS 11+, Windows 10/11 (WSL2) | 官方测试通过的平台，其他 Unix-like 系统可自行适配 |
| 浏览器 | 基于 Chromium 104+ / Firefox 115+ / WebKit 16.4+ | 管理后台前端框架依赖较新的 CSS 与 JavaScript 特性 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户手册 | /docs/user-guide/ | 如何添加链接、创建分类、切换主题、使用检索功能 |
| 管理员指南 | /docs/admin-guide/ | 如何配置健康检测、管理用户权限、执行数据备份与恢复 |
| 部署参考 | /docs/deployment/ | 如何通过 Docker、PM2 或 systemd 将服务部署至生产环境 |
| 开发者文档 | /docs/developer/ | 项目整体架构、API 设计、插件扩展机制与前端组件开发规范 |
| 常见问题 | /docs/faq/ | 高频报错解决方案、性能调优建议及数据迁移注意事项 |

## 资源列表

### 篮球比分类站点

<code>lanqiubifen365.org.cn</code>

<code>lanqiubifen888.org.cn</code>

<code>lanqiubifenbf.org.cn</code>

<code>lanqiubifenw.com.cn</code>

<code>lanqiubifenw.org.cn</code>

<code>lanqiubifenw.net.cn</code>

<code>lanqiubifenbf.net.cn</code>

## 项目结构

```
navsphere/
├── src/
│   ├── api/                     # RESTful API 路由层（含链接、分类、统计、用户模块）
│   ├── core/                    # 核心业务逻辑（数据库连接、健康检测调度、缓存管理）
│   ├── models/                  # 数据模型定义（Link, Category, User, Log 等 ORM 实体）
│   ├── services/                # 服务层（链接导入导出、统计聚合、权限校验）
│   ├── web/                     # 前端页面与组件（基于 EJS 模板 + 原生 JavaScript）
│   │   ├── assets/              # 静态资源（CSS 主题、字体图标、客户端脚本）
│   │   ├── layouts/             # 页面布局骨架（含头部导航、侧边栏、底部）
│   │   └── views/               # 页面模板（首页、分类视图、管理后台、登录注册）
│   └── worker/                  # 后台任务（定时健康检测、日志归档、统计刷新）
├── config/                      # 环境配置（默认参数、数据库连接字符串、日志级别）
├── data/                        # 数据存储目录（SQLite 数据库文件、备份归档、导入暂存）
├── test/                        # 单元测试与集成测试（基于 Mocha + Chai）
├── docs/                        # 完整项目文档（含用户手册、管理员指南、API 参考）
├── scripts/                     # 工具脚本（数据库初始化、迁移、种子数据填充）
├── .env.example                 # 环境变量示例（含 JWT 密钥、检测间隔、端口号）
├── Dockerfile                   # 多阶段构建镜像定义（基于 Alpine Linux）
├── docker-compose.yml           # 本地开发与演示环境编排（含 Redis 可选依赖）
├── package.json                 # npm 项目清单（依赖、脚本、元信息）
├── README.md                    # 项目入口说明文档（即本文档）
└── LICENSE                      # MIT 许可证文本
```

## 贡献指南

1. **提交 Issue 描述需求或缺陷**：请使用 GitHub Issue 模板，清晰说明复现步骤、预期行为与实际表现，并附上环境信息（操作系统、Node 版本、浏览器版本）。
2. **Fork 仓库并创建特性分支**：从主分支 `main` 切出 `feature/xxx` 或 `fix/xxx` 分支，确保分支名简洁反映改动内容。
3. **编写或更新测试用例**：对于新增功能或修复，需补充对应的单元测试或集成测试，保证代码覆盖率不低于原有水平。
4. **遵循代码风格规范**：JavaScript/Node 代码使用 ESLint 配置（基于 Airbnb 风格），提交前执行 `npm run lint` 与 `npm run format` 进行自动格式化。
5. **发起 Pull Request 并等待审核**：描述改动背景、实现方案及测试结果，至少需一名项目维护者 Approve 后方可合并。合并后 CI 将自动构建并部署预览版本。

## 常见问题

**Q：健康检测误报链接不可达，如何调整超时或重试策略？**

A：可在 `config/default.yaml` 中修改 `healthCheck.timeout`（单位毫秒）和 `healthCheck.retries` 参数。若为内网或跨国站点，建议适当增加超时值并启用重试机制。修改后需重启服务生效。

**Q：导入大量链接时页面卡顿或超时，应如何优化？**

A：推荐将导入文件拆分为每批 500 条以内的多个文件，并使用后台任务模式（通过 `npm run import:batch` 命令）执行异步导入。同时可启用 Redis 缓存减轻数据库压力，具体配置参考 `docker-compose.redis.yml`。

**Q：如何从旧版本迁移数据到新版本？**

A：执行 `npm run db:export` 导出当前数据为 JSON 备份，升级代码后运行 `npm run db:migrate` 自动执行增量迁移脚本，最后使用 `npm run db:import` 恢复数据。若迁移失败，可回滚至备份文件并联系维护者获取支持。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:44:11
