# DeJiao Resource Hub

DeJiao Resource Hub 是一个面向中文互联网技术社区与站点管理者的外链资源聚合与导航系统。该项目定位于对分散的垂直领域信息源进行结构化整理与可读化呈现，帮助技术调研人员、站点运营者及内容研究者快速定位目标资源，降低信息检索与站点发现的时间成本。通过标准化的元数据描述与资源分类体系，项目提供了一套清晰、可扩展的站点导航框架，适用于个人或小型团队构建专属的垂直领域入口。

项目本身不依赖动态后端，完全基于静态 Markdown 与 JSON 数据构建，可无缝部署于任意支持静态托管的平台。其核心价值在于将零散的域名资源转化为有序、可维护的知识资产，并为后续的自动化监控、可用性检测与内容更新提供坚实的数据基础。

## 功能概览

- **静态化资源索引**：所有站点数据以纯文本形式存储，无需数据库支持，便于版本管理与跨设备同步。
- **分类标签体系**：支持为每个资源赋予多个类别标签，实现按主题、机构或内容类型的多维度筛选。
- **Markdown 渲染引擎**：项目内嵌轻量级 Markdown 解析组件，可将资源描述、使用说明等富文本信息转换为标准 HTML 页面。
- **自动化站点状态检测**：集成定时任务脚本，周期性检查收录站点的 HTTP 响应状态码，并生成可用性报告。
- **响应式导航页面**：前端模板采用移动优先的响应式设计，确保在桌面端与移动设备上均获得良好的浏览体验。
- **资源导入导出**：支持通过 CSV 或 JSON 格式批量导入外部链接数据，同时可导出为结构化文档供其他系统使用。
- **变更历史追踪**：通过 Git 提交记录与变更日志文件，完整保留每次资源增删改的操作上下文。

## 应用场景

- **技术调研与竞品分析**：研究人员可通过本系统快速访问特定领域的多个相关站点，对比不同平台的内容结构与服务模式，无需逐一记忆或搜索域名。
- **垂直领域知识库构建**：社区维护者可将项目作为基础框架，持续收录与某技术主题（例如编程语言、开源协议、算法竞赛）相关的资源链接，形成领域专属入口。
- **内部团队书签管理**：中小型开发团队可将部署后的实例用作内部共享书签墙，统一存放常用文档、工具平台与监控面板地址，减少重复询问与查找时间。
- **开源项目外链展示页**：开源项目维护者可利用本系统为其项目生态中的依赖库、工具链、社区论坛等外部资源提供一份结构化导航，提升项目文档的完整度。

## 快速开始

以下步骤适用于 Linux/macOS 及 Windows WSL 环境。请确保已安装 Git 与 Node.js 运行时。

```bash
# 克隆项目仓库
git clone https://github.com/dejiao-hub/resource-hub.git
cd resource-hub

# 安装依赖（使用 npm）
npm install

# 构建静态资源并启动本地预览服务
npm run build
npm start
```

执行完成后，访问 `http://localhost:3000` 即可查看导航页面。若需自定义资源列表，请编辑 `data/resources.json` 文件并重新构建。

## 安装要求

项目运行所需的环境依赖与最低版本要求如下表所示：

| 依赖组件 | 必需版本 | 说明 |
|----------|----------|------|
| Node.js | 18.x 或更高 | 运行时环境，用于执行构建脚本与本地服务器 |
| npm | 9.x 或更高 | 包管理工具，用于安装项目依赖项 |
| Git | 2.30 或更高 | 版本控制工具，用于克隆仓库及提交变更 |
| 操作系统 | Linux / macOS / Windows WSL2 | 支持主流 POSIX 环境，Windows 需使用 WSL |
| 浏览器 | 支持 ES6 的现代浏览器 | 用于预览导航页面，推荐 Chrome / Firefox 最新版 |
| 网络 | 可访问公网 | 用于首次安装依赖及检测收录站点状态 |

## 文档导航

项目文档按照不同使用角色与关注点进行分层组织，便于快速找到所需信息：

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | `docs/user-guide/` | 如何使用导航页面进行资源检索与筛选？如何提交资源变更建议？ |
| 维护指南 | `docs/maintainer-guide/` | 如何添加、编辑或删除资源条目？如何运行状态检测脚本？ |
| 开发参考 | `docs/developer-guide/` | 项目目录结构如何理解？前端模板变量有哪些？如何扩展分类体系？ |
| 部署说明 | `docs/deployment/` | 支持哪些托管平台？如何配置自定义域名与 HTTPS？ |

## 资源列表

本项目收录的外部资源链接按类别整理如下。所有链接均严格保持用户提供的原始格式，不作任何修饰或转换。

**核心域名单组**

- <code>dejiabifenwang.org.cn</code>
- <code>dejiabifensaicheng.org.cn</code>
- <code>dejiabifen.org.cn</code>

**关联专题站点**

- <code>danchaosaicheng.org.cn</code>
- <code>danchaojishibifen.org.cn</code>
- <code>danchaojifenbang.org.cn</code>
- <code>danchaobifen.org.cn</code>

## 项目结构

项目目录采用模块化分层设计，核心代码与数据文件清晰分离，便于维护与扩展。

```
resource-hub/
├── config/                      # 全局配置文件目录
│   ├── site.config.js          # 站点基础元数据（标题、描述、作者）
│   └── category.mapping.js     # 分类标签与显示名称的映射规则
├── data/                        # 核心数据存储目录
│   ├── resources.json          # 所有收录站点的完整结构化数据（主数据文件）
│   ├── tags.json               # 预定义的标签集合及使用频次统计
│   └── changelog.md            # 资源列表变更历史记录（按时间倒序）
├── src/                         # 源代码目录
│   ├── core/                   # 核心处理逻辑模块
│   │   ├── validator.js        # URL 格式校验与规范化辅助函数
│   │   └── parser.js           # 从 CSV/JSON 导入数据的解析器
│   ├── templates/              # 前端模板引擎文件
│   │   ├── layout.hbs          # 全局布局模板（含头部、尾部、导航栏）
│   │   └── partials/           # 可复用的模板片段（卡片、列表、筛选器）
│   └── assets/                 # 静态资源目录
│       ├── styles/             # CSS 样式文件（含响应式断点定义）
│       └── scripts/            # 前端交互脚本（筛选、搜索、状态标记）
├── scripts/                     # 工具脚本与自动化任务
│   ├── health-check.js         # 周期性检测各站点 HTTP 可用性
│   └── generate-sitemap.js     # 生成站点地图文件供搜索引擎使用
├── dist/                        # 构建输出目录（自动生成，不纳入版本控制）
├── test/                        # 单元测试与集成测试用例
│   ├── validator.test.js       # 针对核心函数的基础测试套件
│   └── fixtures/               # 测试用模拟数据
├── .gitignore                   # Git 忽略文件规则
├── package.json                 # 项目依赖声明与脚本入口
├── README.md                    # 项目总览与使用说明（即本文档）
└── LICENSE                      # MIT 许可协议全文
```

## 贡献指南

我们欢迎并感谢任何形式的贡献，包括但不限于新增资源链接、修正已有数据错误、改进前端界面与文档。请遵循以下步骤参与协作：

1. **提交 Issue 讨论**：在发起 Pull Request 之前，请先在 Issues 区域描述您希望进行的变更或新增内容，以便维护者与其他贡献者确认方案合理性，避免重复工作。
2. **派生项目并创建分支**：将本仓库 Fork 至您的个人账户，然后在本地仓库中基于 `main` 分支创建一个描述性的新分支，例如 `add-resource-2026` 或 `fix-typo-readme`。
3. **修改数据文件并本地验证**：编辑 `data/resources.json` 或其他相关文件，然后运行 `npm test` 确保所有检查通过，最后执行 `npm run build` 确认构建无误。
4. **提交变更并创建 Pull Request**：提交清晰的 commit 信息，详细说明变更内容与原因，然后向本仓库的 `main` 分支发起 Pull Request。请在请求中引用相关 Issue 编号。
5. **代码审查与合并**：维护者将在收到请求后进行审查，可能会提出修改建议。通过后由维护者完成合并，您的贡献将被记录在贡献者列表中。

## 常见问题

**问：项目是否支持在线添加或删除资源，而不需要重新构建？**

答：当前版本为纯静态架构，每次修改 `data/resources.json` 后均需手动执行 `npm run build` 重新生成页面。若您需要动态管理功能，可以考虑集成后端 API 或使用 CMS 方案，但这不在本项目默认支持范围内。

**问：如何自定义导航页面的 logo 与配色方案？**

答：您可以直接替换 `src/assets/images/` 目录下的 logo 文件，并修改 `src/assets/styles/variables.css` 中定义的 CSS 自定义属性（如 `--primary-color`、`--background-color`）。所有样式变量均集中管理，便于全局统一调整。

**问：收录的站点如果无法访问，系统会如何处理？**

答：项目自带的 `scripts/health-check.js` 脚本会定期检测所有收录站点的 HTTP 状态码。若某站点连续三次检测均返回非 200 状态，系统会在 `data/resources.json` 中为该条目增加 `unreachable` 标记，并在导航页面上以视觉弱化方式显示，但不会自动删除该条目，以保留历史记录供维护者判断。

## 许可证

本项目采用 MIT 许可证。您被允许自由使用、复制、修改、合并、发布、分发、再许可及销售本软件的副本，但需在软件副本中保留原始版权声明与许可声明。本软件按“现状”提供，不提供任何明示或默示的担保，包括但不限于适销性、特定用途适用性及非侵权性的保证。详情请参阅项目根目录下的 LICENSE 文件。

> 外链数量: 7 | 生成时间: 2026-08-11 03:44:16
