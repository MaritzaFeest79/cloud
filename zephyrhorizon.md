# NexusArchive

NexusArchive 是一个面向技术决策者与基础设施工程师的外部资源聚合与导航系统。项目定位为“只读型技术资源索引”，不生成原创内容，不提供计算服务，专注于对特定领域外部链接进行结构化整理、分类标注与可用性监测。目标用户包括运维工程师、技术选型负责人、开源项目维护者以及需要快速定位特定垂直领域原始数据源的分析人员。本项目解决的核心问题是外部技术资源分散、域名记忆成本高、链接失效难以追踪，以及团队内部共享外部链接时缺乏统一入口与状态标记机制。

## 功能概览

- **按领域分级的资源目录树**：提供多级分类视图，将外部链接按业务域、数据用途、服务类型进行划分，支持快速过滤与批量导出。
- **链接存活状态自动检测**：每日定时对收录的全部外部域名进行 HTTP/HTTPS 可用性探测，标记异常状态并生成变动日志。
- **原始域名严格保真存储**：保留用户提交的每一原始 URL 字符串，不自动补全协议、不修改大小写、不附加尾部斜杠，确保与上游数据源完全一致。
- **变更历史审计追踪**：记录每一条链接的新增、删除、状态变更操作，支持按时间范围回溯，满足内部合规审查要求。
- **轻量级只读 API 接口**：提供基于 JSON 的资源列表查询接口，支持按分类、状态、关键字筛选，便于其他系统集成。
- **静态化文档生成**：支持将当前资源列表与状态快照导出为静态 Markdown 或 HTML 页面，适用于离线分发或文档归档。
- **多用户只读访问控制**：支持基于令牌的只读访问，可区分内部团队与公开访客权限，隐藏内部备注字段。

## 应用场景

- **基础设施选型时的外部数据源核查**：技术团队在评估第三方服务或数据供应商时，可通过 NexusArchive 快速获取该领域已收录的原始域名列表，避免遗漏关键候选源，同时通过状态检测功能初步判断各服务端可用性。
- **开源项目文档中的外部依赖索引**：开源项目维护者可将 NexusArchive 作为项目 README 或 Wiki 的外部链接附录源，自动同步更新，减少手动维护链接列表的人力成本。
- **内部知识库的链接网关**：企业可将 NexusArchive 部署为内部知识库的前置链接网关，所有外部引用均先经过本系统登记，当外部域名变更或失效时，仅需在 NexusArchive 中更新，无需修改每一篇文档。
- **安全研究中的域名资产梳理**：安全分析人员可利用本项目的分类视图与导出功能，批量获取特定类别的域名列表，用于威胁情报比对或暴露面分析，确保原始 URL 不被改写以保证溯源准确性。
- **运维巡检中的依赖健康检查**：运维团队可将本项目状态检测 API 集成至巡检脚本，在常规基础设施检查中一并验证业务所依赖的全部外部资源可达性，提前发现上游中断风险。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，要求已安装 Git、Node.js 20.x 及以上版本、pnpm 8.x 及以上版本。

```bash
# 1. 克隆仓库
git clone https://github.com/nexusarchive/nexusarchive.git
cd nexusarchive

# 2. 安装项目依赖
pnpm install

# 3. 复制示例环境变量文件并配置
cp .env.example .env.local

# 4. 执行初始资源同步（从内置种子数据导入）
pnpm run seed:init

# 5. 启动开发服务器，默认监听 3000 端口
pnpm run dev
```

启动后，访问 <code>http://localhost:3000</code> 即可查看资源列表主页。生产环境部署请参考 `docs/deployment.md` 使用 Docker 或 systemd 方式。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 20.x 或 22.x LTS | 运行时环境，推荐使用官方二进制或 nvm 安装 |
| pnpm | 8.x 或 9.x | 包管理器，本项目不使用 npm 或 yarn |
| PostgreSQL | 14.x 或 15.x | 主数据库，用于存储资源列表、状态历史与审计日志 |
| Redis | 7.x | 缓存层，用于状态检测结果的短期存储与 API 频率控制 |
| Git | 2.30+ | 版本控制，用于克隆仓库及提交贡献 |
| curl / wget | 任意现代版本 | 可选，用于手动调用状态检测脚本或 API 测试 |

## 文档导航

| 层面 | 目录路径 | 回答的问题 |
|------|----------|------------|
| 用户手册 | `docs/user-guide/` | 如何使用分类浏览、搜索、导出功能；如何解读状态标识；如何配置个人只读令牌 |
| 管理员手册 | `docs/admin-guide/` | 如何新增或禁用外部链接；如何调整检测频率；如何查看审计日志；如何执行批量导入导出 |
| 开发者指南 | `docs/developer-guide/` | 项目整体架构设计；核心数据模型说明；如何扩展新的检测协议；如何编写单元测试与集成测试 |
| 运维部署 | `docs/deployment/` | 环境变量完整列表；Docker Compose 生产配置；反向代理示例（Nginx / Caddy）；备份与恢复策略 |

完整文档根目录位于 `docs/`，API 接口文档由 Swagger 自动生成，开发模式下访问 <code>http://localhost:3000/api-docs</code> 可查看。

## 资源列表

### 足球数据预测类

- <code>zuqiudsyuce.com.cn</code>
- <code>zuqiudsyuce.cn</code>

### 足球推荐分析类

- <code>zuqiudstuijian.net.cn</code>
- <code>zuqiudstuijian.cn</code>
- <code>zuqiudstuijian.com.cn</code>

### 足球数据统计类

- <code>zuqiudsshuju.net.cn</code>
- <code>zuqiudsshuju.org.cn</code>

## 项目结构

```
nexusarchive/
├── apps/
│   └── web/                         # 主应用前端及 API 路由（Next.js App Router）
│       ├── app/                     # 页面路由与布局组件
│       ├── api/                     # RESTful API 端点（资源查询、状态检测、审计）
│       └── components/              # 可复用的 UI 组件（资源卡片、分类树、状态徽标）
├── packages/
│   ├── core/                        # 核心数据模型、验证规则、类型定义（TypeScript）
│   ├── detector/                    # 链接可用性检测引擎（支持 HTTP/HTTPS 及重定向跟踪）
│   ├── parser/                      # 原始 URL 解析与规范化工具（严格保真存储逻辑）
│   └── cli/                         # 命令行工具（手动触发检测、导入导出、数据迁移）
├── scripts/
│   ├── seed/                        # 初始种子数据（含本次 7 个链接的预置分类）
│   └── migration/                   # 数据库迁移脚本（基于 Kysely 或 Prisma）
├── tests/
│   ├── unit/                        # 单元测试（Jest / Vitest）
│   └── integration/                 # 集成测试（测试数据库与 API 全链路）
├── docs/                            # 完整文档（见上文文档导航）
├── .env.example                     # 环境变量模板
├── docker-compose.yml               # 本地开发及生产容器编排
├── package.json                     # 项目依赖与脚本定义
└── README.md                        # 本文件
```

## 贡献指南

1. **查阅问题追踪器**：访问 GitHub Issues 查看已标记的 `help wanted` 或 `good first issue` 标签任务，在任务下留言表明认领意向，避免重复工作。
2. **派生仓库并创建功能分支**：将主仓库 Fork 至个人账户，然后克隆派生仓库，基于 `main` 分支创建命名规范的功能分支，例如 `feat/add-detector-timeout` 或 `fix/url-preserve-case`。
3. **编写变更与对应测试**：确保新增代码包含充分的单元测试及必要的集成测试，所有检测逻辑变更需附带手动测试用例说明。严格遵守项目的 TypeScript 严格模式与 ESLint 规则，提交前执行 `pnpm run lint` 与 `pnpm run test`。
4. **提交符合规范的变更说明**：提交信息遵循 Conventional Commits 格式，正文需清晰描述变更动机、实现方式及影响范围。若涉及数据库变更，必须同时提供回滚脚本。
5. **发起拉取请求**：推送分支后向主仓库 `main` 分支发起 Pull Request，填写模板中的检查清单，等待至少一位核心维护者审阅。审阅通过后由维护者合并，合并后自动触发 CI 构建与部署至预发布环境。

## 常见问题

**问：为什么某些链接显示为不可达，但我手动访问浏览器却能打开？**

答：本项目的检测引擎默认使用 HEAD 请求且遵循严格超时设置（连接超时 5 秒，总耗时 10 秒），不执行客户端 JavaScript 也不携带浏览器特有的 Cookie 或用户代理扩展头。若目标服务器对非浏览器请求返回非 2xx/3xx 状态码，或响应时间超出阈值，则标记为异常。您可以在管理员后台调整检测参数，或通过 API 获取详细响应头部与状态码用于排查。

**问：如何批量导入我自己的外部链接列表，而不用逐条手动添加？**

答：项目支持 CSV 与 JSON 两种批量导入格式。您需要准备包含 `url`、`category`、`description` 三列的文件，然后使用 CLI 命令 `pnpm run cli import --format=csv --file=./my-list.csv` 执行导入。导入前建议先使用 `--dry-run` 参数进行校验，系统会报告格式错误或重复条目，但不实际写入数据库。导入后所有新链接的状态检测将在下一个检测周期自动执行。

**问：本项目是否提供高可用部署方案，防止单点故障？**

答：是的。生产部署推荐使用 Docker Compose 或 Kubernetes Helm 图表，支持多副本运行。PostgreSQL 可配置主从复制，Redis 支持哨兵模式。状态检测任务使用分布式锁确保同一时刻仅一个实例执行检测，避免重复请求。详细的 HA 配置参数请参考 `docs/deployment/ha.md` 文档。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
