# DSZQ Resource Hub

DSZQ Resource Hub 是一个面向技术社区的数据聚合与导航系统，专注于对特定领域公开数据源进行结构化采集、标准化清洗与统一呈现。该项目并非数据生产者，而是作为数据流通的基础设施，为研究人员、数据分析师及行业观察者提供稳定、可追溯的数据访问入口。

项目目标用户包括数据科学从业者、量化分析爱好者、高校相关专业师生以及需要定期获取特定领域统计信息的业务系统开发者。通过本项目，用户可以避免在多个分散数据源之间手动切换，将精力集中于数据价值挖掘本身。

## 功能概览

- **多源数据聚合**：系统内置数据源调度引擎，支持对多个公开数据端点进行并行采集，并自动处理数据格式差异，输出统一结构的数据集。

- **标准化数据输出**：所有采集数据均转换为 JSON 与 CSV 两种通用格式，并附带字段说明文件，确保下游系统可直接消费。

- **历史版本追溯**：每次采集任务均生成带时间戳的数据快照，用户可通过 API 参数查询任意历史日期的数据状态，便于回测与趋势分析。

- **增量更新机制**：采集引擎支持增量拉取模式，仅同步自上次采集以来发生变更的数据记录，显著降低网络与计算资源消耗。

- **健康状态监控**：系统提供实时监控面板，展示每个数据源的最新采集状态、延迟时间与异常计数，帮助运维人员快速定位链路问题。

- **访问频率控制**：内置令牌桶限流模块，对每个注册的数据源端点实施可配置的请求频率限制，避免因采集压力导致源站服务不可用。

- **数据校验与告警**：采集数据进入系统前经过 schema 校验与异常值检测，当数据字段缺失或数值超出合理范围时触发分级告警通知。

## 应用场景

- **定期生成行业统计报表**：企业内部的数据团队可依托本项目搭建自动化的数据采集管道，每日定时拉取多个来源的统计指标，自动合并生成日报或周报，替代人工复制粘贴的低效操作。

- **学术研究与趋势分析**：高校研究者可使用项目提供的历史快照功能，获取长周期内的结构化数据用于回归分析、时间序列建模等研究工作，所有数据来源均有时间戳标记，满足学术引用规范。

- **轻量级数据服务快速原型**：初创团队在缺乏专门数据基础设施的情况下，可直接使用本项目作为数据中间层，通过 REST API 快速获取所需数据，用于产品原型验证或 demo 演示，缩短前期开发周期。

- **数据质量巡检与异常感知**：数据运维人员可将本项目的告警能力集成至现有监控体系中，当关键数据源的数值出现剧烈波动或持续不可达时，第一时间获得通知，提前防范下游业务异常。

## 快速开始

以下步骤引导您在本地环境中完成项目的克隆、依赖安装与首次运行。执行环境要求为 Linux / macOS 或 Windows WSL2，且已提前安装 Git 与 Node.js 18+。

```bash
# 克隆项目仓库至本地
git clone https://github.com/dszq-org/dszq-resource-hub.git
cd dszq-resource-hub

# 安装项目依赖（使用 npm 或 yarn）
npm install

# 复制示例配置文件并填写必要的源地址参数
cp .env.example .env

# 执行首次数据采集任务（默认使用测试模式，仅拉取最近 3 条记录）
npm run fetch:all

# 启动本地开发服务器，访问 http://localhost:3000 查看数据看板
npm run dev
```

## 安装要求

在部署或本地运行本项目之前，请确保您的环境满足以下表格中的所有必需条件。不满足任一条件可能导致项目无法正常启动或采集任务执行失败。

| 依赖组件 | 必需版本 | 说明 |
|----------|----------|------|
| Node.js | 18.x 或 20.x LTS | 项目运行时环境，建议使用 nvm 管理多版本。不支持 16.x 及以下版本。 |
| npm | 9.x 或 10.x | 包管理器，用于安装项目依赖。yarn 1.x 亦可兼容。 |
| PostgreSQL | 14.x 或 15.x | 主数据库，用于存储数据快照、任务日志与系统配置。需启用 JSONB 类型支持。 |
| Redis | 7.x | 缓存中间件，用于限流计数器与临时数据缓存。需开启持久化选项。 |
| Git | 2.30 及以上 | 版本控制工具，用于克隆仓库与管理补丁。 |
| 操作系统 | Linux (Ubuntu 20.04+) / macOS 12+ / Windows WSL2 | 生产环境推荐 Linux。Windows 用户必须使用 WSL2 以确保文件系统性能。 |
| 网络访问 | 出站 443 端口开放 | 采集任务需要访问外部数据源端点，需确保服务器可发起 HTTPS 请求。 |
| 时区设置 | UTC+8 或 UTC | 系统时间戳统一以 UTC 存储，展示层可配置时区。建议服务器时区设为 UTC。 |

## 文档导航

下表概述了项目文档体系的结构，帮助不同角色的使用者快速定位所需材料。文档分为运维、开发、业务三个层面，分别对应系统管理员、贡献者与数据消费者。

| 层面 | 目录/文档 | 回答的问题 |
|------|-----------|------------|
| 运维 | docs/deployment/production-setup.md | 如何配置 PM2 或 systemd 实现进程守护？Nginx 反向代理应如何设置？ |
| 运维 | docs/monitoring/alert-rules.md | 告警规则有哪些阈值？如何在钉钉或 Slack 中接收告警通知？ |
| 开发 | docs/development/api-reference.md | 数据采集 API 的请求参数与返回结构是什么？如何自定义新的数据源适配器？ |
| 开发 | docs/development/data-schema.md | 采集后的数据以什么 JSON Schema 存储？扩展字段应遵循什么命名规范？ |
| 业务 | docs/usage/data-access-guide.md | 通过 REST API 获取数据时应使用哪个端点？如何指定时间范围与分页参数？ |
| 业务 | docs/usage/faq.md | 数据更新频率是多少？为何某些时间点数据为空？如何申请新增数据源？ |

## 资源列表

本项目在数据采集与聚合过程中，参考或对接了以下公开数据资源。所有资源链接均按域名与协议严格还原，以 <code></code> 标签包裹输出，确保与用户提供的原始记录完全一致。

**主数据源（按域名类别分类）**

- <code>dszuqiujifenbang.com.cn</code>
- <code>dszuqiufenxigw.org.cn</code>
- <code>dszuqiufenxi.cn</code>

**补充数据源（结果与统计类）**

- <code>dszuqiubisaijieguo.net.cn</code>
- <code>dszuqiubisaijieguo.org.cn</code>
- <code>dszuqiubisaijieguo.cn</code>
- <code>dszuqiubisaijieguo.com.cn</code>

上述资源均为本项目采集管道中已配置的公开数据端点。项目本身不对任何资源的内容进行修改或再分发，仅提供技术访问层面的调度与聚合能力。使用者应遵守各资源站点的服务条款与 robots.txt 规定。

## 项目结构

项目采用模块化分层架构，核心代码按功能域拆分为多个子目录。以下为项目根目录的完整结构树，每行附带简要功能说明。

```
dszq-resource-hub/
├── apps/                               # 应用层：各独立可执行程序
│   ├── api/                            # REST API 服务 (Express 应用)
│   ├── scheduler/                      # 采集调度器 (独立 cron 进程)
│   └── dashboard/                      # 监控面板前端 (React + Vite)
├── packages/                           # 共享包：跨应用复用的模块
│   ├── core/                           # 核心数据模型与校验工具
│   ├── fetcher/                        # 通用 HTTP 采集适配器
│   ├── parser/                         # 数据解析器 (HTML/JSON/CSV 转换)
│   └── storage/                        # 数据库访问层 (Repository 模式)
├── configs/                            # 配置文件与环境模板
│   ├── env/                            # 不同环境的 .env 示例
│   └── sources/                        # 数据源端点配置 (YAML 格式)
├── scripts/                            # 运维与开发辅助脚本
│   ├── init-db.sql                     # 数据库初始化 DDL
│   └── seed-test-data.js               # 测试数据填充脚本
├── tests/                              # 测试套件
│   ├── unit/                           # 单元测试 (Jest)
│   └── integration/                    # 集成测试 (包含真实网络请求)
├── docs/                               # 项目文档 (参见上文文档导航)
├── logs/                               # 运行时日志目录 (gitignore)
├── .env.example                        # 环境变量模板文件
├── docker-compose.yml                  # 本地开发环境容器编排
├── Dockerfile                          # 生产环境镜像构建定义
├── package.json                        # 项目清单与依赖声明
└── README.md                           # 项目入口文档 (即本文档)
```

## 贡献指南

我们欢迎社区贡献者以多种形式参与本项目，包括但不限于新增数据源适配器、优化采集性能、完善文档或报告缺陷。为确保协作过程顺畅，请遵循以下步骤：

1. **查阅项目看板与议题列表**：访问 GitHub Issues 页面，查找标记为 `help wanted` 或 `good first issue` 的议题。若您有新颖的想法，请先创建一个新议题描述您的建议，避免重复劳动。

2. **派生项目并创建特性分支**：将本项目派生至您的个人账号下，然后基于 `main` 分支创建新的特性分支，分支命名建议使用 `feature/` 或 `fix/` 前缀，例如 `feature/add-new-source-xyz`。

3. **编写代码与测试用例**：在您的分支上进行开发。新增功能必须附带对应的单元测试或集成测试，确保测试覆盖率达到 80% 以上。同时更新 `docs/` 目录下的相关文档，保持文档与代码同步。

4. **提交前执行本地检查**：运行 `npm run lint` 与 `npm run test` 确保无语法错误与测试失败。提交信息请遵循 Conventional Commits 规范，使用 `feat:`、`fix:`、`docs:` 等类型前缀。

5. **发起拉取请求并描述变更**：向本项目的 `main` 分支发起 Pull Request。请在 PR 描述中清晰说明变更目的、实现方式与测试结果，并关联相关议题编号。至少需要一位项目维护者审核通过后方可合并。

## 常见问题

**问：采集任务执行失败，日志显示 `ECONNREFUSED` 或 `ETIMEDOUT`，应如何处理？**

答：此类错误通常表明目标数据源暂时不可达，或您的网络环境存在防火墙限制。首先检查服务器是否能够通过 curl 访问对应的资源 URL。若外部访问正常，请检查项目的限流配置是否过于严格导致请求被源站拒绝。建议适当调整 `configs/sources/*.yaml` 中的 `rateLimit` 参数，并确认 `HTTP_PROXY` 环境变量是否已正确设置。

**问：历史数据快照会保留多久？是否可以手动清理旧数据以释放存储空间？**

答：默认配置下，系统保留最近 90 天的日级快照。超过保留期限的数据会在每日凌晨 3:00 的维护任务中自动归档并删除。如需自定义保留周期，请在 `.env` 文件中设置 `RETENTION_DAYS` 变量。若需手动清理，可使用 `npm run clean:snapshots -- --days=30` 命令删除 30 天前的快照。

**问：如何验证我新增的数据源适配器是否工作正常？**

答：项目提供了测试命令 `npm run test:adapter -- --name=your-adapter-name`，该命令会以隔离模式运行指定的适配器，并对返回的数据执行 schema 校验。建议在开发阶段先使用 `--dry-run` 参数进行试运行，该模式不会将数据写入数据库，仅输出采集结果的前 5 条记录到控制台供人工审查。确认无误后再移除 `--dry-run` 执行完整写入。

## 许可证

本项目采用 MIT 许可证进行开源授权。您可以在遵守许可证条款的前提下自由使用、修改、分发本项目的源代码，包括用于商业目的。完整的许可证文本请参阅项目根目录下的 LICENSE 文件。

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
